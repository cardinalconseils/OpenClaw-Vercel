# Railway Nixpacks build configuration
[phases.setup]
nixPkgs = ["nodejs_23", "git", "curl", "jq", "bash"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "bash scripts/startup.sh"
