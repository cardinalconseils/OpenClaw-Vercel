---
phase: 01-infrastructure-foundation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server.ts
  - bin/sandbox-start.sh
autonomous: true
gap_closure: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-04
  - INFRA-05

must_haves:
  truths:
    - "Running npx tsx src/server.ts starts Express on port 18790 and begins accepting requests"
    - "server.ts CLI entrypoint instantiates GatewayManager, starts it, and starts the keep-alive loop"
    - "sandbox-start.sh delegates gateway lifecycle to server.ts instead of raw nohup — GatewayManager and keep-alive are active in production"
  artifacts:
    - path: "src/server.ts"
      provides: "CLI entrypoint that starts Express, GatewayManager, and keep-alive"
      contains: "startServer"
    - path: "bin/sandbox-start.sh"
      provides: "Startup script using server.ts as the managed entrypoint"
  key_links:
    - from: "src/server.ts"
      to: "src/startup/gateway-manager.ts"
      via: "import GatewayManager and call start()"
      pattern: "new GatewayManager"
    - from: "src/server.ts"
      to: "src/startup/keepalive.ts"
      via: "import startKeepAlive and call with GatewayManager instance"
      pattern: "startKeepAlive"
    - from: "bin/sandbox-start.sh"
      to: "src/server.ts"
      via: "npx tsx src/server.ts"
      pattern: "npx tsx src/server.ts"
---

<objective>
Wire the runtime entrypoint so that the Express server, GatewayManager, and keep-alive loop all start when `npx tsx src/server.ts` is executed.

Purpose: Close two verification gaps — (1) server.ts exports startServer() but never calls it when run directly, so Express never binds a port; (2) GatewayManager and keep-alive are implemented and tested but orphaned from any runtime path. Both modules must be wired into server.ts CLI entrypoint and sandbox-start.sh must delegate gateway lifecycle to server.ts.

Output: Working CLI entrypoint in server.ts, updated sandbox-start.sh
</objective>

<execution_context>
@/Users/pmc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pmc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/01-infrastructure-foundation/01-02-SUMMARY.md
@.planning/phases/01-infrastructure-foundation/01-03-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/startup/gateway-manager.ts:
```typescript
export class GatewayManager {
  start(): void;
  async stop(): Promise<void>;
  async restart(): Promise<void>;
  async isHealthy(): Promise<boolean>;
  getProcess(): ChildProcess | null;
}
```

From src/startup/keepalive.ts:
```typescript
export function startKeepAlive(gatewayManager: GatewayManager): NodeJS.Timeout;
export function stopKeepAlive(timer: NodeJS.Timeout): void;
```

From src/server.ts (current):
```typescript
export const app: Express;
export function startServer(port?: number): ReturnType<typeof app.listen>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CLI entrypoint to server.ts that wires GatewayManager and keep-alive</name>
  <files>src/server.ts</files>
  <action>
Modify src/server.ts to add a CLI entrypoint block at the bottom of the file. When the module is executed directly (via `npx tsx src/server.ts` or compiled `node server.js`), it must:

1. Detect direct execution using `process.argv[1]` endsWith check for both `.ts` and `.js` extensions (per project decision in STATE.md).

2. In the CLI block:
   a. Import and instantiate `GatewayManager` from `./startup/gateway-manager.js`
   b. Call `gatewayManager.start()` to spawn the OpenClaw gateway with auto-restart
   c. Wait for gateway to become healthy — poll `gatewayManager.isHealthy()` in a loop (max 30 attempts, 1 second apart). Log progress. If gateway is not healthy after 30s, log error and exit with code 1.
   d. Call `startServer()` to bind Express on port 18790
   e. Import and call `startKeepAlive(gatewayManager)` from `./startup/keepalive.js` to start the 5-minute health check loop
   f. Log "[server] Infrastructure ready — Express + GatewayManager + keep-alive active"

3. Add SIGTERM/SIGINT handler in the CLI block that calls `stopKeepAlive()`, `gatewayManager.stop()`, and `server.close()` for graceful shutdown.

IMPORTANT: Keep all existing exports (`app`, `startServer`) unchanged — they are used by tests via supertest. The CLI entrypoint is additive only.

IMPORTANT: Use top-level imports for GatewayManager and keepalive (not dynamic imports) so TypeScript can validate the types. The CLI entrypoint block only controls whether they are instantiated and called.

Per project decision: "Register exit listener before kill() in restart()/stop() to avoid missing synchronous exit event" — this is already handled in GatewayManager. No changes needed there.
  </action>
  <verify>
    <automated>npx vitest run tests/api/webhooks.test.ts</automated>
  </verify>
  <done>
    - `npx tsx src/server.ts` starts Express, GatewayManager, and keep-alive (does not exit immediately)
    - Existing tests still pass (app and startServer exports unchanged)
    - Graceful shutdown handler registered for SIGTERM/SIGINT
  </done>
</task>

<task type="auto">
  <name>Task 2: Update sandbox-start.sh to delegate gateway lifecycle to server.ts</name>
  <files>bin/sandbox-start.sh</files>
  <action>
Update bin/sandbox-start.sh to remove the raw `nohup openclaw start` and manual health-check loop, since server.ts now handles gateway startup via GatewayManager and keep-alive.

New startup sequence:

1. Phase 1 (unchanged): Pre-pair device — `npx tsx src/startup/pair-device.ts`

2. Phase 2 (changed): Start server.ts as the single managed process. server.ts now handles:
   - Starting the gateway via GatewayManager (with auto-restart + backoff)
   - Waiting for gateway health (with 30s timeout)
   - Starting Express on port 18790
   - Starting keep-alive loop
   Run: `npx tsx src/server.ts &`
   Save PID as `SERVER_PID`

3. Phase 3 (changed): Wait for Express to become healthy — poll `curl -sf http://127.0.0.1:18790/health` in a loop (max 45 attempts, 1 second apart). The timeout is longer (45s) because server.ts first waits up to 30s for the gateway, then starts Express.

4. Phase 4 (unchanged): Update Telnyx webhook URL — `npx tsx src/startup/webhook-url-updater.ts`

5. Phase 5 (changed): Log readiness with just SERVER_PID (no separate GATEWAY_PID).
   `wait $SERVER_PID` at the end.

Remove: The old `nohup openclaw start` block, the old gateway health-check loop (Phase 2+3 in old script), and the separate EXPRESS_PID tracking.

Keep: The `set -euo pipefail` at the top, the pair-device step, the webhook-url-updater step.
  </action>
  <verify>
    <automated>bash -n bin/sandbox-start.sh && echo "Syntax OK"</automated>
  </verify>
  <done>
    - sandbox-start.sh has valid bash syntax
    - Gateway lifecycle is managed by server.ts (GatewayManager + keep-alive), not raw nohup
    - Only one background process (server.ts) instead of two (gateway + express)
    - Webhook URL update still runs after Express is healthy
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run` — all existing tests pass (no regressions)
2. `bash -n bin/sandbox-start.sh` — startup script has valid syntax
3. `grep -q "startKeepAlive" src/server.ts` — keep-alive is wired
4. `grep -q "GatewayManager" src/server.ts` — GatewayManager is wired
5. `grep -q "startServer" src/server.ts` — CLI entrypoint calls startServer
</verification>

<success_criteria>
- Running `npx tsx src/server.ts` starts Express, spawns the gateway via GatewayManager, and activates the keep-alive loop (all three were previously disconnected)
- sandbox-start.sh runs a single managed process (server.ts) instead of two unmanaged ones
- All existing tests continue to pass
- Both VERIFICATION.md gaps are addressed: server.ts CLI entrypoint (Gap 1) and keep-alive/GatewayManager runtime wiring (Gap 2)
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-foundation/01-04-SUMMARY.md`
</output>
