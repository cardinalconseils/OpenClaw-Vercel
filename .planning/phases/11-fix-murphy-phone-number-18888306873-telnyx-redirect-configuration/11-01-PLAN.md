---
phase: 11-fix-murphy-phone-number-18888306873-telnyx-redirect-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/startup/openclaw-config.ts
autonomous: false
requirements:
  - FIX-01
  - FIX-02

must_haves:
  truths:
    - "Calls to +18888306873 forward to ClawdTalk +18885440160 and Murphy answers"
    - "SMS sent by Murphy during ClawdTalk calls arrives to the caller"
    - "TELNYX_PUBLIC_KEY in Vercel production matches the canonical value from Telnyx API"
    - "Webhook signature verifier handles pre-parsed request bodies without producing [object Object]"
  artifacts:
    - path: "src/startup/openclaw-config.ts"
      provides: "ClawdTalk plugin config with sessions_send in tool allowlist"
      contains: "sessions_send"
    - path: "src/lib/voice/webhook-verify.ts"
      provides: "Ed25519 webhook signature verification with robust rawBody handling (VERIFICATION ONLY — confirmed correct in prior commit, not modified by this plan)"
      contains: "JSON.stringify(req.body)"
  key_links:
    - from: "Telnyx +18888306873"
      to: "ClawdTalk +18885440160"
      via: "Telnyx call forwarding configured via MCP tools"
      pattern: "18885440160"
    - from: "src/startup/openclaw-config.ts"
      to: "OpenClaw gateway"
      via: "ClawdTalk plugin config with sessions_send allowlist"
      pattern: "sessions_send"
    - from: "src/lib/voice/webhook-verify.ts"
      to: "TELNYX_PUBLIC_KEY env var"
      via: "process.env.TELNYX_PUBLIC_KEY"
      pattern: "process\\.env\\.TELNYX_PUBLIC_KEY"
---

<objective>
Configure Telnyx call forwarding so Canadian callers on +18888306873 reach Murphy via ClawdTalk (+18885440160), and fix SMS delivery by adding `sessions_send` to the ClawdTalk gateway tool allowlist.

**Primary goals (call forwarding + SMS):**
1. Forward +18888306873 to +18885440160 at the Telnyx level (no webhook involvement)
2. Add `sessions_send` to ClawdTalk gateway tool allowlist so SMS recaps arrive

**Secondary hygiene (FIX-01 / FIX-02):**
3. Verify TELNYX_PUBLIC_KEY in Vercel production matches canonical value (for future direct-Telnyx path — not used by ClawdTalk)
4. Confirm webhook-verify.ts rawBody hardening is already in place (no code change expected — already committed)

Purpose: Murphy is unreachable from the Canadian toll-free number and SMS recaps are not arriving through ClawdTalk calls. The ClawdTalk path is the primary call path per CONTEXT.md; the Telnyx webhook path is retained for future use but is not the focus.
Output: Working call forwarding, working SMS via ClawdTalk, verified env var, deployed config change.
</objective>

<execution_context>
@/Users/pmc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pmc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-fix-murphy-phone-number-18888306873-telnyx-redirect-configuration/11-CONTEXT.md
@.planning/phases/11-fix-murphy-phone-number-18888306873-telnyx-redirect-configuration/11-RESEARCH.md

<interfaces>
<!-- Key contracts the executor needs -->

From src/startup/openclaw-config.ts (lines 72-78):
```typescript
'clawdtalk': {
  enabled: true,
  config: {
    apiKey: process.env.CLAWDTALK_API_KEY ?? '',
    botId: process.env.CLAWDTALK_BOT_ID ?? '',
  },
},
```
NOTE: This config currently has NO tool allowlist. The `sessions_send` tool must be added to enable ClawdTalk SMS.

From src/lib/voice/webhook-verify.ts:
```typescript
export async function telnyxWebhookVerifier(
  req: Request, res: Response, next: NextFunction
): Promise<void>
// rawBody extraction (lines 33-39) already handles Buffer, string, object, and fallback
// The object case uses JSON.stringify(req.body) -- this was already hardened in a prior commit
```

Correct TELNYX_PUBLIC_KEY value (from Telnyx API):
```
wJMd8IPWKTdnZTGghRIxaRScLwo/wQgVNWLvKmrD3Ic=
```

Telnyx Connection ID: 2917100803690858224
ClawdTalk dedicated number: +18885440160
Canadian toll-free: +18888306873
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Telnyx call forwarding and verify env var (primary + secondary hygiene)</name>
  <files>src/startup/openclaw-config.ts</files>
  <read_first>
    - src/lib/voice/webhook-verify.ts (confirm rawBody handling already has JSON.stringify fallback -- read-only verification)
    - src/api/webhooks.ts (middleware stack context)
    - .env.example (env var names for reference)
  </read_first>
  <action>
**Step 1 -- Configure Telnyx call forwarding (+18888306873 to +18885440160) [PRIMARY]:**

Use the Telnyx MCP tools available in the executor environment to configure unconditional call forwarding on the Canadian toll-free number. Execute these steps in order:

1. Use `list_phone_numbers` MCP tool to find +18888306873 and get its current configuration (phone_number_id, connection_id, current forwarding settings).

2. Use `get_phone_number` MCP tool with the phone_number_id to inspect current call forwarding state.

3. Use `update_phone_number` MCP tool to set call forwarding on +18888306873:
   - Set `call_forwarding.forwarding_type` to `"always"` (unconditional)
   - Set `call_forwarding.forwards_to` to `"+18885440160"`
   - If `update_phone_number` does not support call forwarding fields, try `update_connection` on connection ID `2917100803690858224` to set forwarding at the connection level.

4. After the update, use `get_phone_number` MCP tool again to confirm the forwarding is now active. The response MUST show +18885440160 as the forwarding target.

If Telnyx MCP tools do not support call forwarding configuration, fall back to the Telnyx REST API:
```bash
curl -s -X PATCH "https://api.telnyx.com/v2/phone_numbers/{phone_number_id}" \
  -H "Authorization: Bearer $TELNYX_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"call_forwarding":{"call_forwarding_enabled":true,"forwarding_type":"always","forwards_to":"+18885440160"}}'
```

**Step 2 -- Verify TELNYX_PUBLIC_KEY in Vercel production [SECONDARY - FIX-01]:**

This is secondary hygiene for the future direct-Telnyx path (not used by ClawdTalk).

Run `vercel env pull .env.vercel-check.local` to pull production env vars. Compare the `TELNYX_PUBLIC_KEY` value against the canonical value: `wJMd8IPWKTdnZTGghRIxaRScLwo/wQgVNWLvKmrD3Ic=`

If the values differ or TELNYX_PUBLIC_KEY is empty/missing:
1. Run `vercel env rm TELNYX_PUBLIC_KEY production` (confirm when prompted)
2. Run `vercel env add TELNYX_PUBLIC_KEY production` and paste exact value: `wJMd8IPWKTdnZTGghRIxaRScLwo/wQgVNWLvKmrD3Ic=`

After checking, delete the temp file: `rm .env.vercel-check.local`

**Step 3 -- Confirm webhook-verify.ts rawBody hardening [SECONDARY - FIX-02, read-only]:**

Read `src/lib/voice/webhook-verify.ts` and confirm the rawBody extraction already contains all four cases:
1. `req.body instanceof Buffer` -> `.toString('utf8')`
2. `typeof req.body === 'string'` -> use as-is
3. `typeof req.body === 'object' && req.body !== null` -> `JSON.stringify(req.body)`
4. Fallback -> `String(req.body ?? '')`

This was already hardened in a prior commit. No code change expected. Document confirmation in the summary.

**Step 4 -- Run full test suite:**

Run `npm test` to confirm all tests pass (no regressions).
  </action>
  <verify>
    <automated>npm test</automated>
    Telnyx forwarding acceptance: After Step 1, use `get_phone_number` MCP tool (or `curl -s GET https://api.telnyx.com/v2/phone_numbers/{id}`) to confirm the API response includes forwarding to +18885440160.
  </verify>
  <done>
    - Telnyx API confirms +18888306873 forwards to +18885440160 (verified via GET response)
    - TELNYX_PUBLIC_KEY verified/corrected in Vercel production
    - webhook-verify.ts rawBody handling confirmed robust (read-only, no changes needed)
    - npm test exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Add sessions_send to ClawdTalk gateway tool allowlist and deploy</name>
  <files>src/startup/openclaw-config.ts</files>
  <read_first>
    - src/startup/openclaw-config.ts (current ClawdTalk plugin config -- needs sessions_send allowlist)
    - src/server.ts (startup sequence -- config is written before gateway starts)
    - bin/sandbox-start.sh (deployment startup sequence)
  </read_first>
  <action>
**Step 1 -- Add `sessions_send` to ClawdTalk plugin config in openclaw-config.ts:**

In `src/startup/openclaw-config.ts`, update the `clawdtalk` plugin entry (currently lines 72-78) from:

```typescript
'clawdtalk': {
  enabled: true,
  config: {
    apiKey: process.env.CLAWDTALK_API_KEY ?? '',
    botId: process.env.CLAWDTALK_BOT_ID ?? '',
  },
},
```

To:

```typescript
'clawdtalk': {
  enabled: true,
  config: {
    apiKey: process.env.CLAWDTALK_API_KEY ?? '',
    botId: process.env.CLAWDTALK_BOT_ID ?? '',
    allow: ['sessions_send'],
  },
},
```

The `allow` array tells the OpenClaw gateway which tools the ClawdTalk skill is permitted to use. Without `sessions_send` in this list, SMS requests from Murphy during ClawdTalk calls fail silently or return 404.

**Step 2 -- Run tests to confirm no regressions:**

Run `npm test` to verify all tests pass. The openclaw-config.ts tests (if any) should still pass with the added field.

**Step 3 -- Deploy to Vercel production:**

Run `vercel deploy --prod` to deploy the updated gateway config.

Wait for deployment to complete. Verify:
- `curl -s https://murphy.help/health` returns `{"status":"ok"}`

The gateway will pick up the new config on next restart, which happens on each Vercel cold start or sandbox restart.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `openclaw-config.ts` contains `allow: ['sessions_send']` inside the clawdtalk plugin config block
    - `openclaw-config.ts` ClawdTalk config still has `apiKey` and `botId` fields (not removed)
    - `npm test` exits 0 with all tests passing
    - `curl -s https://murphy.help/health` returns `{"status":"ok"}` after deployment
  </acceptance_criteria>
  <done>ClawdTalk gateway config updated with sessions_send allowlist, deployed to production, health endpoint responding</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <files>N/A</files>
  <action>Human verifies the full integration: call forwarding from Canadian toll-free to ClawdTalk, SMS delivery via sessions_send, and health endpoint. No code changes in this task.</action>
  <name>Task 3: Live call and SMS verification</name>
  <what-built>
    1. Configured Telnyx call forwarding: +18888306873 forwards to ClawdTalk +18885440160
    2. Added `sessions_send` to ClawdTalk gateway tool allowlist for SMS delivery
    3. Verified/corrected TELNYX_PUBLIC_KEY in Vercel production (secondary hygiene)
    4. Confirmed webhook-verify.ts has robust rawBody handling (read-only verification)
    5. Deployed all changes to Vercel production
  </what-built>
  <how-to-verify>
    1. **Call forwarding test:** Call +18888306873 from any phone
       - Expected: Murphy answers via ClawdTalk (greeting within 5 seconds)
       - If it rings and drops: forwarding may not be configured correctly -- check Telnyx portal for +18888306873 forwarding settings

    2. **SMS test:** During the call, interact with Murphy enough to trigger an SMS recap (describe a service need, go through the flow)
       - Expected: You receive an SMS after the call ends with a recap
       - If no SMS arrives: check if `sessions_send` is in the gateway logs / ClawdTalk dashboard

    3. **Direct ClawdTalk test (baseline):** Call +18885440160 directly
       - Expected: Murphy answers (this should already work)
       - This confirms ClawdTalk is functional independently of the forwarding

    4. **Health check:** Run `curl -s https://murphy.help/health`
       - Expected: `{"status":"ok"}`
  </how-to-verify>
  <verify>
    <automated>curl -s https://murphy.help/health</automated>
  </verify>
  <done>Live call to +18888306873 forwards to ClawdTalk, Murphy answers, SMS recap arrives after call</done>
  <resume-signal>Type "approved" if both call forwarding and SMS work, or describe what happened (ring-and-drop, no SMS, unexpected behavior)</resume-signal>
</task>

</tasks>

<verification>
1. Telnyx API GET confirms forwarding from +18888306873 to +18885440160 (primary acceptance gate)
2. `openclaw-config.ts` contains `allow: ['sessions_send']` in ClawdTalk plugin config
3. `npm test` passes (all existing tests green)
4. `curl -s https://murphy.help/health` returns `{"status":"ok"}`
5. TELNYX_PUBLIC_KEY in Vercel matches canonical value (secondary hygiene)
6. `webhook-verify.ts` has robust rawBody handling -- confirmed via read, no modification (secondary hygiene)
7. Live call to +18888306873 is answered by Murphy via ClawdTalk
8. SMS recap arrives after a ClawdTalk call
</verification>

<success_criteria>
- Canadian callers dialing +18888306873 reach Murphy through ClawdTalk forwarding (verified by Telnyx API GET + live call)
- SMS recaps are delivered when calls arrive via ClawdTalk (sessions_send enabled)
- TELNYX_PUBLIC_KEY is verified correct in Vercel production (FIX-01 -- secondary hygiene)
- Webhook verifier handles pre-parsed bodies without [object Object] (FIX-02 -- confirmed already in place, read-only)
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/11-fix-murphy-phone-number-18888306873-telnyx-redirect-configuration/11-01-SUMMARY.md`
</output>
