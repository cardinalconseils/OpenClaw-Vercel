---
phase: 10-add-privacy-policy-and-terms-and-conditions-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/landing/footer.tsx
  - frontend/src/app/(auth)/login/page.tsx
  - frontend/src/app/__tests__/landing.test.tsx
autonomous: false
requirements:
  - LEGAL-05
  - LEGAL-06

must_haves:
  truths:
    - "Footer copyright row contains Privacy Policy and Terms of Service links pointing to /privacy and /terms"
    - "Login page CardFooter contains 'By signing in, you agree to our Terms of Service and Privacy Policy' text with links to /terms and /privacy"
    - "Existing landing page tests still pass"
    - "Footer links render correctly in the test suite"
  artifacts:
    - path: "frontend/src/components/landing/footer.tsx"
      provides: "Footer with legal links in copyright row"
      contains: "Privacy Policy"
    - path: "frontend/src/app/(auth)/login/page.tsx"
      provides: "Login page with Terms/Privacy agreement text"
      contains: "Terms of Service"
    - path: "frontend/src/app/__tests__/landing.test.tsx"
      provides: "Tests including footer legal link verification"
      contains: "Privacy Policy"
  key_links:
    - from: "frontend/src/components/landing/footer.tsx"
      to: "/privacy"
      via: "Next.js Link href"
      pattern: 'href="/privacy"'
    - from: "frontend/src/components/landing/footer.tsx"
      to: "/terms"
      via: "Next.js Link href"
      pattern: 'href="/terms"'
    - from: "frontend/src/app/(auth)/login/page.tsx"
      to: "/terms"
      via: "Next.js Link href"
      pattern: 'href="/terms"'
    - from: "frontend/src/app/(auth)/login/page.tsx"
      to: "/privacy"
      via: "Next.js Link href"
      pattern: 'href="/privacy"'
---

<objective>
Integrate Privacy Policy and Terms of Service links into the existing footer and login page, with test coverage.

Purpose: Connect the legal pages to the site navigation so users can find them from any page (footer) and are informed of legal terms before signing in (login page).

Output: Modified footer.tsx with legal links, modified login/page.tsx with agreement text, updated landing tests with footer link assertions.
</objective>

<execution_context>
@/Users/pmc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pmc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-add-privacy-policy-and-terms-and-conditions-pages/10-CONTEXT.md
@.planning/phases/10-add-privacy-policy-and-terms-and-conditions-pages/10-RESEARCH.md

<interfaces>
<!-- Current footer copyright row (lines 57-62 of footer.tsx) -->
From frontend/src/components/landing/footer.tsx:
```tsx
{/* Copyright */}
<div className="border-t border-border pt-6">
  <p className="font-sans text-sm font-normal text-muted-foreground text-center">
    &copy; {year} Murphy. All rights reserved.
  </p>
</div>
```

<!-- Current login CardFooter (lines 169-173 of login/page.tsx) -->
From frontend/src/app/(auth)/login/page.tsx:
```tsx
<CardFooter className="justify-center">
  <p className="text-sm text-muted-foreground">
    Admin access only
  </p>
</CardFooter>
```

<!-- Login page does NOT currently import Link from next/link -->
<!-- Login page currently imports: useState, useRouter, Loader2, createClient, Button, Input, Label, Card/CardContent/CardHeader/CardTitle/CardDescription/CardFooter -->

<!-- Existing test pattern from landing.test.tsx -->
From frontend/src/app/__tests__/landing.test.tsx:
```tsx
import { describe, it, expect } from 'vitest'
import { render, screen } from '@testing-library/react'
// Uses dynamic import: const { Footer } = await import('@/components/landing/footer')
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add legal links to footer copyright row</name>
  <files>frontend/src/components/landing/footer.tsx</files>
  <read_first>
    - frontend/src/components/landing/footer.tsx (current full file — must see exact structure before modifying)
  </read_first>
  <action>
Modify `frontend/src/components/landing/footer.tsx` — replace the copyright `<div>` (lines 57-62) with a flex layout containing copyright text + legal links.

Replace this block:
```tsx
{/* Copyright */}
<div className="border-t border-border pt-6">
  <p className="font-sans text-sm font-normal text-muted-foreground text-center">
    &copy; {year} Murphy. All rights reserved.
  </p>
</div>
```

With this:
```tsx
{/* Copyright */}
<div className="border-t border-border pt-6 flex flex-col sm:flex-row items-center justify-center gap-2 sm:gap-6">
  <p className="font-sans text-sm font-normal text-muted-foreground">
    &copy; {year} Murphy. All rights reserved.
  </p>
  <div className="flex items-center gap-4">
    <Link
      href="/privacy"
      className="font-sans text-sm font-normal text-muted-foreground hover:text-foreground transition-colors"
    >
      Privacy Policy
    </Link>
    <span className="text-muted-foreground/40 text-sm">|</span>
    <Link
      href="/terms"
      className="font-sans text-sm font-normal text-muted-foreground hover:text-foreground transition-colors"
    >
      Terms of Service
    </Link>
  </div>
</div>
```

The file already imports `Link from 'next/link'` on line 1, so no new import is needed.
  </action>
  <verify>
    <automated>cd /Users/pmc/Documents/DEV/OpenClaw-Vercel/frontend && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/landing/footer.tsx contains `href="/privacy"`
    - footer.tsx contains `href="/terms"`
    - footer.tsx contains `Privacy Policy`
    - footer.tsx contains `Terms of Service`
    - footer.tsx contains `flex flex-col sm:flex-row` (responsive layout)
    - footer.tsx still contains `&copy; {year} Murphy. All rights reserved.`
    - Type check passes
  </acceptance_criteria>
  <done>Footer copyright row displays "Privacy Policy | Terms of Service" links inline with copyright text, stacking vertically on mobile</done>
</task>

<task type="auto">
  <name>Task 2: Add Terms/Privacy agreement text to login page</name>
  <files>frontend/src/app/(auth)/login/page.tsx</files>
  <read_first>
    - frontend/src/app/(auth)/login/page.tsx (current full file — must see imports and CardFooter structure)
  </read_first>
  <action>
Modify `frontend/src/app/(auth)/login/page.tsx`:

1. Add `Link` import — add this line after the existing imports (after line 17):
```tsx
import Link from 'next/link'
```

2. Replace the CardFooter block (lines 169-173):

From:
```tsx
<CardFooter className="justify-center">
  <p className="text-sm text-muted-foreground">
    Admin access only
  </p>
</CardFooter>
```

To:
```tsx
<CardFooter className="flex-col gap-2 text-center">
  <p className="text-sm text-muted-foreground">
    Admin access only
  </p>
  <p className="text-xs text-muted-foreground">
    By signing in, you agree to our{' '}
    <Link href="/terms" className="text-primary hover:underline">Terms of Service</Link>
    {' '}and{' '}
    <Link href="/privacy" className="text-primary hover:underline">Privacy Policy</Link>
  </p>
</CardFooter>
```

Note: The CardFooter className changes from `"justify-center"` to `"flex-col gap-2 text-center"` to stack the two paragraphs vertically.
  </action>
  <verify>
    <automated>cd /Users/pmc/Documents/DEV/OpenClaw-Vercel/frontend && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/app/(auth)/login/page.tsx contains `import Link from 'next/link'`
    - login/page.tsx contains `href="/terms"`
    - login/page.tsx contains `href="/privacy"`
    - login/page.tsx contains `By signing in, you agree to our`
    - login/page.tsx contains `Terms of Service`
    - login/page.tsx contains `Privacy Policy`
    - login/page.tsx CardFooter has className `flex-col gap-2 text-center`
    - login/page.tsx still contains `Admin access only`
    - Type check passes
  </acceptance_criteria>
  <done>Login page displays "By signing in, you agree to our Terms of Service and Privacy Policy" below the "Admin access only" text with working links</done>
</task>

<task type="auto">
  <name>Task 3: Add footer legal link tests to landing test suite</name>
  <files>frontend/src/app/__tests__/landing.test.tsx</files>
  <read_first>
    - frontend/src/app/__tests__/landing.test.tsx (current test patterns and imports)
    - frontend/src/components/landing/footer.tsx (modified footer to verify)
  </read_first>
  <action>
Add a new `describe('Footer')` block to `frontend/src/app/__tests__/landing.test.tsx` after the existing `describe('VoiceWave')` block (before the closing `})` of the outer describe).

Add this test block:
```tsx
describe('Footer', () => {
  it('renders Privacy Policy link pointing to /privacy', async () => {
    const { Footer } = await import('@/components/landing/footer')
    render(<Footer />)
    const privacyLink = screen.getByRole('link', { name: /privacy policy/i })
    expect(privacyLink.getAttribute('href')).toBe('/privacy')
  })

  it('renders Terms of Service link pointing to /terms', async () => {
    const { Footer } = await import('@/components/landing/footer')
    render(<Footer />)
    const termsLink = screen.getByRole('link', { name: /terms of service/i })
    expect(termsLink.getAttribute('href')).toBe('/terms')
  })
})
```

This follows the existing test pattern: vitest describe/it, dynamic import of component, render + screen query, href assertion.
  </action>
  <verify>
    <automated>cd /Users/pmc/Documents/DEV/OpenClaw-Vercel/frontend && npm test -- --run src/app/__tests__/landing.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/app/__tests__/landing.test.tsx contains `describe('Footer'`
    - Test file contains `privacy policy` (case-insensitive regex)
    - Test file contains `terms of service` (case-insensitive regex)
    - Test file contains `'/privacy'`
    - Test file contains `'/terms'`
    - `npm test -- --run src/app/__tests__/landing.test.tsx` exits with 0 (all tests pass)
  </acceptance_criteria>
  <done>Landing test suite includes footer legal link assertions, all tests pass including existing ones</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Visual verification of legal pages and integrations</name>
  <files>frontend/src/app/privacy/page.tsx, frontend/src/app/terms/page.tsx, frontend/src/components/landing/footer.tsx, frontend/src/app/(auth)/login/page.tsx</files>
  <action>
Human visual verification checkpoint. Start the frontend dev server if not running (`cd frontend && npm run dev`), then verify the following in the browser:

1. Visit http://localhost:3000/privacy — verify dark-themed Privacy Policy page with sticky ToC sidebar, scroll-spy highlighting, "Last updated: March 17, 2026", attorney disclaimer, all sections present, "Cardinal Conseils" and "info@cardinalconseils.com" in content.
2. Visit http://localhost:3000/terms — verify Terms of Service content, AI Disclosure section references CA SB-1001, describes Murphy as an AI agent.
3. Resize to mobile (<1024px) — verify ToC collapses into expandable "Contents" dropdown on both pages.
4. Check footer on any page — verify "Privacy Policy | Terms of Service" links navigate correctly.
5. Visit http://localhost:3000/login — verify "By signing in, you agree to our Terms of Service and Privacy Policy" text with working links.
  </action>
  <verify>
    <automated>cd /Users/pmc/Documents/DEV/OpenClaw-Vercel/frontend && npm test -- --run 2>&1 | tail -5</automated>
  </verify>
  <done>User has visually confirmed: both legal pages render correctly with proper theme/layout/content, mobile ToC collapses, footer links work, login agreement text displays with working links</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` passes with zero errors
- `cd frontend && npm test -- --run` passes (all existing + new tests)
- Footer renders Privacy Policy and Terms of Service links
- Login page shows agreement text with working links
</verification>

<success_criteria>
- Footer copyright row includes Privacy Policy (/privacy) and Terms of Service (/terms) links
- Login page shows "By signing in, you agree to our Terms of Service and Privacy Policy" with links
- New footer tests pass in the landing test suite
- All existing tests remain green
- Visual verification confirms correct rendering on desktop and mobile
</success_criteria>

<output>
After completion, create `.planning/phases/10-add-privacy-policy-and-terms-and-conditions-pages/10-02-SUMMARY.md`
</output>
