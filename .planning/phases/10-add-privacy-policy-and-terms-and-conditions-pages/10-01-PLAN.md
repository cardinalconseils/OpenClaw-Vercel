---
phase: 10-add-privacy-policy-and-terms-and-conditions-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/legal/legal-page-layout.tsx
  - frontend/src/components/legal/legal-toc.tsx
  - frontend/src/app/privacy/page.tsx
  - frontend/src/app/terms/page.tsx
autonomous: true
requirements:
  - LEGAL-01
  - LEGAL-02
  - LEGAL-03
  - LEGAL-04
user_setup: []

must_haves:
  truths:
    - "Visiting /privacy shows a full Privacy Policy page with Cardinal Conseils entity name, CCPA/PIPEDA/TCPA/CAN-SPAM coverage, and all six third-party services disclosed"
    - "Visiting /terms shows a full Terms of Service page with AI Disclosure section referencing CA SB-1001"
    - "Both pages have a sticky table of contents sidebar on desktop that highlights the active section while scrolling"
    - "Both pages display a 'Last updated' date and attorney disclaimer at the top"
    - "On mobile (<1024px), the table of contents collapses into an expandable details/summary element"
  artifacts:
    - path: "frontend/src/components/legal/legal-page-layout.tsx"
      provides: "Shared two-column grid layout with sticky ToC sidebar"
      contains: "LegalPageLayout"
    - path: "frontend/src/components/legal/legal-toc.tsx"
      provides: "Client component with IntersectionObserver scroll-spy"
      contains: "use client"
    - path: "frontend/src/app/privacy/page.tsx"
      provides: "Privacy Policy page at /privacy"
      contains: "Cardinal Conseils"
    - path: "frontend/src/app/terms/page.tsx"
      provides: "Terms of Service page at /terms"
      contains: "CA SB-1001"
  key_links:
    - from: "frontend/src/app/privacy/page.tsx"
      to: "frontend/src/components/legal/legal-page-layout.tsx"
      via: "import LegalPageLayout"
      pattern: "import.*LegalPageLayout.*from.*legal-page-layout"
    - from: "frontend/src/app/terms/page.tsx"
      to: "frontend/src/components/legal/legal-page-layout.tsx"
      via: "import LegalPageLayout"
      pattern: "import.*LegalPageLayout.*from.*legal-page-layout"
    - from: "frontend/src/components/legal/legal-page-layout.tsx"
      to: "frontend/src/components/legal/legal-toc.tsx"
      via: "import LegalToc"
      pattern: "import.*LegalToc.*from.*legal-toc"
---

<objective>
Create the Privacy Policy and Terms of Service pages for murphy.help with shared legal page layout components.

Purpose: Legal compliance pages covering CCPA, PIPEDA, TCPA, CAN-SPAM, and AI disclosure (CA SB-1001) for the Murphy AI phone concierge service operated by Cardinal Conseils.

Output: Four new files — shared layout component, scroll-spy ToC component, /privacy page, /terms page.
</objective>

<execution_context>
@/Users/pmc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pmc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-add-privacy-policy-and-terms-and-conditions-pages/10-CONTEXT.md
@.planning/phases/10-add-privacy-policy-and-terms-and-conditions-pages/10-RESEARCH.md

<interfaces>
<!-- Existing components to reuse on legal pages -->

From frontend/src/components/landing/navbar.tsx:
```tsx
export function NavBar() { ... }
// Client component with Murphy logo, nav links, mobile sheet menu
```

From frontend/src/components/landing/footer.tsx:
```tsx
export function Footer() { ... }
// Server component with 3-column grid, nav links, BuyMeACoffee, copyright row
```

From frontend/src/app/layout.tsx:
```tsx
// Root layout loads Montserrat (--font-montserrat) and Cormorant_Garamond (--font-cormorant)
// ThemeProvider with defaultTheme="dark"
// Font classes: font-sans = Montserrat, font-display = Cormorant Garamond
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared legal page layout and scroll-spy ToC components</name>
  <files>frontend/src/components/legal/legal-page-layout.tsx, frontend/src/components/legal/legal-toc.tsx</files>
  <read_first>
    - frontend/src/app/layout.tsx (font variables and theme setup)
    - frontend/src/app/globals.css (design tokens and CSS variables)
    - frontend/src/components/landing/navbar.tsx (NavBar import path)
    - frontend/src/components/landing/footer.tsx (Footer import path)
  </read_first>
  <action>
Create two new files in `frontend/src/components/legal/`:

**legal-toc.tsx** (client component):
- Add `'use client'` directive at top
- Export `LegalToc` component accepting `{ sections: { id: string; label: string }[], mobile?: boolean }`
- Use `useState` for `activeId` tracking
- Use `useEffect` with `IntersectionObserver` to observe all section elements by their `id`
- IntersectionObserver config: `{ rootMargin: '-80px 0px -60% 0px', threshold: 0 }` — the -80px top margin accounts for the fixed navbar height (64px + buffer)
- When a section intersects, set it as `activeId`
- Desktop mode: render a `<nav aria-label="Table of contents">` with a "Contents" heading (`font-sans font-bold text-xs uppercase tracking-widest text-muted-foreground mb-3`) and a `<ul>` of anchor links
- Active link: `text-primary font-medium`, inactive: `text-muted-foreground hover:text-foreground`
- Mobile mode (`mobile={true}`): render a `<details>` element with `<summary>Contents</summary>` and the same link list inside. Style details with `border border-border rounded-lg p-4`
- Links use `<a href={#${id}}>` (not Next.js Link — same-page anchors)

**legal-page-layout.tsx** (server component, NO `'use client'`):
- Export `LegalPageLayout` component accepting `{ sections: { id: string; label: string }[], title: string, lastUpdated: string, children: React.ReactNode }`
- Import `LegalToc` from `./legal-toc`
- Import `NavBar` from `@/components/landing/navbar`
- Import `Footer` from `@/components/landing/footer`
- Render structure:
  1. `<NavBar />`
  2. `<main className="min-h-screen bg-background pt-24 pb-16 px-4">`
  3. Inside main: `<div className="max-w-7xl mx-auto">`
  4. Page header: `<h1>` with `font-display font-bold text-4xl md:text-5xl text-foreground` for the title
  5. `<p>` with `mt-2 text-sm text-muted-foreground` showing "Last updated: {lastUpdated}"
  6. `<p>` with `mt-1 text-xs text-muted-foreground italic` showing "This document was generated for informational purposes. Consult an attorney for legal advice."
  7. Two-column grid: `grid grid-cols-1 lg:grid-cols-[240px_1fr] gap-12`
  8. Left column (desktop only): `<aside className="hidden lg:block"><div className="sticky top-24"><LegalToc sections={sections} /></div></aside>`
  9. Mobile ToC (above content): `<div className="lg:hidden mb-6"><LegalToc sections={sections} mobile /></div>`
  10. Right column: `<article>` wrapping `{children}` — apply legal typography utility classes via a wrapper div with classes for consistent text styling
  11. `<Footer />`
  </action>
  <verify>
    <automated>cd /Users/pmc/Documents/DEV/OpenClaw-Vercel/frontend && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/legal/legal-toc.tsx exists and contains `'use client'`
    - legal-toc.tsx contains `IntersectionObserver`
    - legal-toc.tsx contains `rootMargin: '-80px 0px -60% 0px'`
    - legal-toc.tsx contains `aria-label="Table of contents"`
    - legal-toc.tsx contains `<details` (mobile mode)
    - legal-page-layout.tsx does NOT contain `'use client'`
    - legal-page-layout.tsx contains `LegalPageLayout`
    - legal-page-layout.tsx contains `sticky top-24`
    - legal-page-layout.tsx contains `lg:grid-cols-[240px_1fr]`
    - legal-page-layout.tsx contains `Consult an attorney for legal advice`
    - legal-page-layout.tsx imports NavBar from `@/components/landing/navbar`
    - legal-page-layout.tsx imports Footer from `@/components/landing/footer`
  </acceptance_criteria>
  <done>Both legal layout components exist, type-check passes, layout renders NavBar + sticky ToC sidebar + content area + Footer</done>
</task>

<task type="auto">
  <name>Task 2: Create Privacy Policy page at /privacy</name>
  <files>frontend/src/app/privacy/page.tsx</files>
  <read_first>
    - frontend/src/components/legal/legal-page-layout.tsx (layout component just created)
    - frontend/src/components/legal/legal-toc.tsx (section type definition)
    - .planning/phases/10-add-privacy-policy-and-terms-and-conditions-pages/10-RESEARCH.md (section structure table)
    - .planning/phases/10-add-privacy-policy-and-terms-and-conditions-pages/10-CONTEXT.md (locked decisions)
  </read_first>
  <action>
Create `frontend/src/app/privacy/page.tsx` as a React Server Component (NO `'use client'`):

1. Add Next.js metadata export:
```tsx
export const metadata: Metadata = {
  title: 'Privacy Policy — Murphy',
  description: 'How Cardinal Conseils collects, uses, and protects your personal data when you use Murphy.',
}
```

2. Define `LAST_UPDATED = 'March 17, 2026'` as a named constant at the top of the file.

3. Define `TOC_SECTIONS` array with these exact entries (in order):
   - `{ id: 'overview', label: 'Overview' }`
   - `{ id: 'data-collected', label: 'Data We Collect' }`
   - `{ id: 'how-we-use', label: 'How We Use Your Data' }`
   - `{ id: 'third-parties', label: 'Third-Party Services' }`
   - `{ id: 'data-retention', label: 'Data Retention' }`
   - `{ id: 'your-rights', label: 'Your Rights' }`
   - `{ id: 'tcpa', label: 'Telephone and SMS Consent' }`
   - `{ id: 'can-spam', label: 'Email Communications' }`
   - `{ id: 'security', label: 'Data Security' }`
   - `{ id: 'contact', label: 'Contact Us' }`

4. Render `<LegalPageLayout sections={TOC_SECTIONS} title="Privacy Policy" lastUpdated={LAST_UPDATED}>` wrapping all content sections.

5. Each section uses `<section id="{id}">` with the matching kebab-case ID from TOC_SECTIONS.

6. Typography classes for content elements:
   - Section headings: `<h2 className="font-display font-bold text-2xl text-foreground mt-10 mb-4" id="{id}">`
   - Sub-headings: `<h3 className="font-sans font-bold text-base text-foreground mt-6 mb-2">`
   - Body paragraphs: `<p className="font-sans text-sm leading-7 text-muted-foreground mb-4">`
   - Inline links: `<a href="..." className="text-primary hover:underline">`
   - Lists: `<ul className="list-disc pl-6 space-y-2 mb-4">` with `<li className="font-sans text-sm leading-7 text-muted-foreground">`

7. **Content requirements per section** (Claude drafts comprehensive legal copy):

   **Overview**: Cardinal Conseils operates Murphy, an AI-powered phone concierge. Describe the service scope. State the policy applies to murphy.help website and phone service.

   **Data We Collect**: Phone number (caller ID), spoken name, service requests (transcribed speech), call recordings (for quality), account email (if registered), approximate location (from speech or IP). Note: data collected automatically via Telnyx telephony and voluntarily via website.

   **How We Use Your Data**: Matching with service providers, executing outbound calls on user's behalf, sending SMS recaps, improving AI service quality, account management.

   **Third-Party Services**: Disclose ALL six services with what data is shared:
   - Telnyx: voice calls, SMS, phone number, call recordings
   - Supabase: account data, call history, mission records (PostgreSQL database, authentication)
   - Google Places API: location and service type queries (no personal data sent)
   - OpenRouter / Anthropic: transcribed speech for AI processing (no persistent storage by providers)
   - BuyMeACoffee: tip link only, no user data shared unless user voluntarily tips
   - Vercel: website hosting, server logs (IP addresses, request metadata)

   **Data Retention**: Call history retained for account lifetime. Account deletion removes all associated data within 30 days. Call recordings retained for 90 days then auto-deleted.

   **Your Rights**:
   - CCPA (California residents): right to know, delete, opt-out of sale (we do not sell data), non-discrimination
   - PIPEDA (Canadian residents): right to access, correct, withdraw consent
   - How to exercise: email info@cardinalconseils.com

   **Telephone and SMS Consent (TCPA)**: Verbal consent captured during call before any SMS is sent. User can opt out of SMS at any time by replying STOP. Outbound calls made only on user's behalf after consent. No telemarketing calls.

   **Email Communications (CAN-SPAM)**: Service emails only (account notifications). Unsubscribe mechanism in all emails. No commercial email marketing.

   **Data Security**: Encryption at rest (Supabase), TLS in transit, access controls, no plain-text password storage.

   **Contact Us**: Cardinal Conseils, info@cardinalconseils.com.
  </action>
  <verify>
    <automated>cd /Users/pmc/Documents/DEV/OpenClaw-Vercel/frontend && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/app/privacy/page.tsx exists
    - File does NOT contain `'use client'`
    - File contains `export const metadata: Metadata`
    - File contains `title: 'Privacy Policy — Murphy'`
    - File contains `Cardinal Conseils`
    - File contains `info@cardinalconseils.com`
    - File contains `LAST_UPDATED = 'March 17, 2026'`
    - File contains `Consult an attorney for legal advice` (via LegalPageLayout) or in the metadata/content
    - File contains all 10 section IDs: `id="overview"`, `id="data-collected"`, `id="how-we-use"`, `id="third-parties"`, `id="data-retention"`, `id="your-rights"`, `id="tcpa"`, `id="can-spam"`, `id="security"`, `id="contact"`
    - File mentions all 6 third-party services: Telnyx, Supabase, Google Places, OpenRouter, Anthropic, BuyMeACoffee, Vercel
    - File contains `CCPA` and `PIPEDA` and `TCPA` and `CAN-SPAM`
    - Type check passes (tsc --noEmit exits 0)
  </acceptance_criteria>
  <done>Privacy Policy page renders at /privacy with all 10 sections, comprehensive legal content covering CCPA/PIPEDA/TCPA/CAN-SPAM, all third-party services disclosed, Cardinal Conseils entity name, and contact email</done>
</task>

<task type="auto">
  <name>Task 3: Create Terms of Service page at /terms</name>
  <files>frontend/src/app/terms/page.tsx</files>
  <read_first>
    - frontend/src/components/legal/legal-page-layout.tsx (layout component)
    - frontend/src/app/privacy/page.tsx (content formatting pattern to follow)
    - .planning/phases/10-add-privacy-policy-and-terms-and-conditions-pages/10-RESEARCH.md (section structure table)
    - .planning/phases/10-add-privacy-policy-and-terms-and-conditions-pages/10-CONTEXT.md (locked decisions)
  </read_first>
  <action>
Create `frontend/src/app/terms/page.tsx` as a React Server Component (NO `'use client'`):

1. Add Next.js metadata export:
```tsx
export const metadata: Metadata = {
  title: 'Terms of Service — Murphy',
  description: 'Terms and conditions for using Murphy, the AI phone concierge by Cardinal Conseils.',
}
```

2. Define `LAST_UPDATED = 'March 17, 2026'` as a named constant.

3. Define `TOC_SECTIONS` array with these exact entries (in order):
   - `{ id: 'overview', label: 'About Murphy' }`
   - `{ id: 'ai-disclosure', label: 'AI Disclosure (CA SB-1001)' }`
   - `{ id: 'eligibility', label: 'Eligibility' }`
   - `{ id: 'service-description', label: 'How the Service Works' }`
   - `{ id: 'acceptable-use', label: 'Acceptable Use' }`
   - `{ id: 'third-party', label: 'Third-Party Services' }`
   - `{ id: 'disclaimers', label: 'Disclaimers' }`
   - `{ id: 'liability', label: 'Limitation of Liability' }`
   - `{ id: 'changes', label: 'Changes to Terms' }`
   - `{ id: 'contact', label: 'Contact Us' }`

4. Use same `<LegalPageLayout>` wrapper and typography classes as the Privacy Policy page.

5. **Content requirements per section** (Claude drafts comprehensive legal copy):

   **About Murphy**: Cardinal Conseils ("we", "us", "our") operates Murphy, an AI-powered phone concierge service. Describe the service: users call a phone number, describe what they need, and the AI agent searches for local service providers, calls them, and connects the caller live. Accepting these terms is required for use.

   **AI Disclosure (CA SB-1001)** — THIS IS THE PROMINENT SECTION per user decision:
   - Murphy is an artificial intelligence agent, not a human operator
   - Murphy makes automated telephone calls to service providers on the user's behalf
   - Per California SB-1001, Murphy identifies itself as an AI to all parties it contacts
   - Murphy does not attempt to deceive any person into believing they are communicating with a human
   - All outbound calls include a verbal AI identity disclosure as the first utterance when a provider answers
   - This is consistent with FCC regulations regarding automated calling systems

   **Eligibility**: Must be 18+ years old. Service available in the United States and Canada.

   **How the Service Works**: Step-by-step description:
   1. User calls the Murphy phone number
   2. AI agent greets the user and captures their service request via natural speech
   3. Agent searches Google Places and other sources for matching providers
   4. Agent calls providers sequentially, checking availability
   5. When a willing provider is found, agent performs a live warm transfer
   6. After the call, user receives an SMS recap with provider info and a tip link

   **Acceptable Use**: Users must not: use the service for fraudulent service requests, abuse the outbound calling system, attempt to use Murphy to harass or spam providers, misrepresent their identity or service needs, use the service for any illegal purpose.

   **Third-Party Services**: Murphy connects users with independent third-party service providers. Cardinal Conseils does not employ, endorse, or guarantee any provider. Google Places results are subject to Google's terms. BuyMeACoffee tips are voluntary and processed by BuyMeACoffee Inc.

   **Disclaimers**: Service provided "as is" and "as available". No guarantee of provider availability, quality, or pricing. Murphy does not schedule, book, or negotiate on behalf of users. AI responses may contain errors. Cardinal Conseils is not liable for actions of third-party service providers.

   **Limitation of Liability**: Standard SaaS limitation — in no event shall Cardinal Conseils be liable for indirect, incidental, special, consequential, or punitive damages. Total liability limited to amounts paid by user in the 12 months preceding the claim (note: service is currently free, so effectively zero).

   **Changes to Terms**: Cardinal Conseils reserves the right to modify these terms. Material changes will be posted on this page with an updated "Last updated" date. Continued use after changes constitutes acceptance.

   **Contact Us**: Cardinal Conseils, info@cardinalconseils.com.
  </action>
  <verify>
    <automated>cd /Users/pmc/Documents/DEV/OpenClaw-Vercel/frontend && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/app/terms/page.tsx exists
    - File does NOT contain `'use client'`
    - File contains `export const metadata: Metadata`
    - File contains `title: 'Terms of Service — Murphy'`
    - File contains `Cardinal Conseils`
    - File contains `info@cardinalconseils.com`
    - File contains `LAST_UPDATED = 'March 17, 2026'`
    - File contains `CA SB-1001` (AI disclosure section)
    - File contains `artificial intelligence` or `AI agent`
    - File contains all 10 section IDs: `id="overview"`, `id="ai-disclosure"`, `id="eligibility"`, `id="service-description"`, `id="acceptable-use"`, `id="third-party"`, `id="disclaimers"`, `id="liability"`, `id="changes"`, `id="contact"`
    - File contains `18` (age requirement)
    - File contains `as is` (disclaimer language)
    - Type check passes (tsc --noEmit exits 0)
  </acceptance_criteria>
  <done>Terms of Service page renders at /terms with all 10 sections, prominent AI disclosure referencing CA SB-1001, comprehensive terms covering service description/acceptable use/disclaimers/liability, Cardinal Conseils entity name, and contact email</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` passes with zero errors
- `cd frontend && npm test -- --run` passes (existing tests unaffected)
- `/privacy` route resolves to a page with Privacy Policy heading
- `/terms` route resolves to a page with Terms of Service heading
</verification>

<success_criteria>
- Two new legal pages exist at /privacy and /terms
- Both pages use the shared LegalPageLayout with sticky ToC sidebar
- Privacy Policy covers CCPA, PIPEDA, TCPA, CAN-SPAM and discloses all 6 third-party services
- Terms of Service has prominent AI Disclosure section referencing CA SB-1001
- Both pages display Cardinal Conseils as operating entity with info@cardinalconseils.com contact
- Both pages show "Last updated: March 17, 2026" and attorney disclaimer
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/10-add-privacy-policy-and-terms-and-conditions-pages/10-01-SUMMARY.md`
</output>
