---
phase: 02-voice-conversation-core
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/ai/prompts/murphy-system.ts
  - src/lib/ai/prompts/voice-modifiers.ts
  - src/lib/ai/intent-extractor.ts
  - tests/lib/ai/prompts/murphy-system.test.ts
  - tests/lib/ai/intent-extractor.test.ts
autonomous: true
requirements:
  - VOICE-02
  - VOICE-03

must_haves:
  truths:
    - "Murphy system prompt includes bilingual language rules (respond in caller's language)"
    - "Murphy system prompt enforces ONE clarifying question max (not the old 2-turn limit)"
    - "Intent extractor parses service type and location from natural utterances"
    - "Intent extractor detects urgency keywords (emergency, urgent, ASAP)"
    - "Ambiguous input produces a disambiguation response with service category options"
  artifacts:
    - path: "src/lib/ai/prompts/murphy-system.ts"
      provides: "Updated Murphy prompt with bilingual + 1-question limit"
      contains: "ONE clarifying question"
    - path: "src/lib/ai/prompts/voice-modifiers.ts"
      provides: "Voice modifiers with bilingual response directive"
      contains: "Language Rules"
    - path: "src/lib/ai/intent-extractor.ts"
      provides: "Intent extraction from transcripts"
      exports: ["extractIntent", "IntentResult"]
    - path: "tests/lib/ai/intent-extractor.test.ts"
      provides: "Intent extraction tests"
  key_links:
    - from: "src/lib/ai/intent-extractor.ts"
      to: "IntentResult type"
      via: "export"
      pattern: "export interface IntentResult"
    - from: "src/lib/ai/prompts/murphy-system.ts"
      to: "ONE clarifying question"
      via: "prompt text"
      pattern: "ONE clarifying question"
---

<objective>
Update Murphy's system prompt with bilingual language rules and a 1-question clarification limit (down from 2), and create the intent extractor that parses service type, location, and urgency from natural speech transcripts.

Purpose: Murphy must respond in the caller's language and never ask more than one clarifying question. The intent extractor is the structured output layer that converts free-form speech into actionable dispatch parameters.
Output: Updated prompt files, new intent-extractor module, all tests passing.
</objective>

<execution_context>
@/Users/pmc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pmc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-voice-conversation-core/02-CONTEXT.md
@.planning/phases/02-voice-conversation-core/02-RESEARCH.md

<interfaces>
<!-- Existing code the executor must read and modify -->

From src/lib/ai/prompts/murphy-system.ts:
```typescript
export function buildMurphySystemPrompt(context: MurphyContext = {}): string
// MurphyContext: { callerPhone?: string; isVoiceCall?: boolean }
// Current prompt has "2-turn clarification maximum" which must change to 1
// Current greeting asks "Who am I speaking with?" which must change to "What service can I help you find today?"
```

From src/lib/ai/prompts/voice-modifiers.ts:
```typescript
export function applyVoiceModifiers(basePrompt: string): string
// Appends voice-mode constraints (no markdown, short sentences)
```

From src/lib/ai/orchestrator.ts:
```typescript
export type TaskType = 'greeting' | 'intent-capture' | 'filler' | 'status-update' | 'disambiguation' | 'provider-ranking' | 'transfer-logic' | 'unknown';
export async function chat(messages: Array<{ role: string; content: string }>, task: TaskType): Promise<string>
```

From tests/lib/ai/prompts/murphy-system.test.ts:
```typescript
// Existing tests check for "AI" in first sentence, tool names, pipeline stages
// New tests must not break existing assertions
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Murphy system prompt with bilingual rules and 1-question clarification limit</name>
  <files>
    src/lib/ai/prompts/murphy-system.ts,
    src/lib/ai/prompts/voice-modifiers.ts,
    tests/lib/ai/prompts/murphy-system.test.ts
  </files>
  <read_first>
    src/lib/ai/prompts/murphy-system.ts,
    src/lib/ai/prompts/voice-modifiers.ts,
    tests/lib/ai/prompts/murphy-system.test.ts,
    .planning/phases/02-voice-conversation-core/02-CONTEXT.md
  </read_first>
  <action>
    **Modify src/lib/ai/prompts/murphy-system.ts:**

    1. Add `language?: 'en' | 'fr'` to the `MurphyContext` interface.

    2. Change the greeting line in the base prompt from:
       `"Hi, I'm Murphy — an AI assistant from OpenClaw Service Matchmaker. Who am I speaking with?"`
       to:
       `"Hi, I'm Murphy — an AI assistant from OpenClaw Service Matchmaker. What service can I help you find today?"`
       (Per CONTEXT.md locked decision — ask for service need, not caller name.)

    3. Change `## Conversation Rules` section: replace `2-turn clarification maximum` with:
       ```
       - ONE clarifying question maximum — if still unclear after one attempt, make a best-guess and proceed to search.
       - Never ask more than one clarifying question in a single call.
       ```

    4. Add a new section after `## Conversation Rules`:
       ```
       ## Language Rules
       - Detect the caller's language from their first utterance.
       - Respond in the same language for the entire call — English or French.
       - Do not mix languages within a single response.
       - If unclear, default to English and offer: "I can also help you in French — just let me know."
       ```

    5. Add a new section after `## Language Rules`:
       ```
       ## Confirmation Pattern
       - After capturing service type and location, confirm before searching: "Got it — a [service] in [location]. Let me find the best options."
       - In French: "Compris — un [service] a [location]. Laissez-moi chercher les meilleures options."
       ```

    6. Add a new section:
       ```
       ## Call Timeout
       - If the call reaches 10 minutes, say: "I want to be respectful of your time — let me wrap up what we've found."
       ```

    **Modify src/lib/ai/prompts/voice-modifiers.ts:**

    Add to the voiceRules string:
    ```
    - Respond in the caller's detected language (English or French) for the entire call.
    - If the caller speaks French, respond entirely in French. Do not switch to English mid-conversation.
    ```

    **Update tests/lib/ai/prompts/murphy-system.test.ts:**

    Keep all existing tests. Add new tests:
    - `it('contains ONE clarifying question rule (not 2-turn)')` — prompt contains "ONE clarifying question" and does NOT contain "2-turn clarification"
    - `it('contains Language Rules section')` — prompt contains "## Language Rules"
    - `it('contains French response instruction')` — prompt contains "French"
    - `it('asks for service need in greeting, not caller name')` — prompt contains "What service" and does NOT contain "Who am I speaking with"
    - `it('contains 10-minute call timeout rule')` — prompt contains "10 minutes"
    - `it('accepts language in MurphyContext')` — `buildMurphySystemPrompt({ language: 'fr' })` returns a string (no type error)

    Run `npx vitest run tests/lib/ai/prompts/murphy-system.test.ts` — all tests pass.
  </action>
  <verify>
    <automated>npx vitest run tests/lib/ai/prompts/murphy-system.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - murphy-system.ts contains "ONE clarifying question"
    - murphy-system.ts does NOT contain "2-turn clarification maximum"
    - murphy-system.ts contains "## Language Rules"
    - murphy-system.ts contains "What service can I help you find today?"
    - murphy-system.ts does NOT contain "Who am I speaking with?"
    - murphy-system.ts contains "10 minutes"
    - murphy-system.ts MurphyContext interface contains `language?: 'en' | 'fr'`
    - voice-modifiers.ts contains "caller's detected language"
    - murphy-system.test.ts contains at least 6 new test cases beyond original 14
    - `npx vitest run tests/lib/ai/prompts/murphy-system.test.ts` exits 0
  </acceptance_criteria>
  <done>Murphy's system prompt enforces 1-question clarification (down from 2), includes bilingual language rules, asks for service need (not name) in greeting, and has a 10-minute timeout rule. Voice modifiers include bilingual directive. All existing and new tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create intent extractor module with tests</name>
  <files>
    src/lib/ai/intent-extractor.ts,
    tests/lib/ai/intent-extractor.test.ts
  </files>
  <read_first>
    src/lib/ai/orchestrator.ts,
    src/lib/tools/registry.ts,
    .planning/phases/02-voice-conversation-core/02-RESEARCH.md
  </read_first>
  <behavior>
    intent-extractor.test.ts:
    - extractIntent("I need a plumber in Austin") returns { serviceType: "plumber", location: "Austin", urgency: "normal", isComplete: true }
    - extractIntent("plumber near downtown Montreal") returns { serviceType: "plumber", location: "downtown Montreal", urgency: "normal", isComplete: true }
    - extractIntent("emergency electrician in 78701") returns { serviceType: "electrician", location: "78701", urgency: "emergency", isComplete: true }
    - extractIntent("I need help with my house") returns { serviceType: undefined, location: undefined, urgency: "normal", isComplete: false }
    - extractIntent("plumber") returns { serviceType: "plumber", location: undefined, urgency: "normal", isComplete: false }
    - extractIntent("j'ai besoin d'un plombier a Montreal") returns { serviceType: "plombier", location: "Montreal", urgency: "normal", isComplete: true }
    - extractIntent("it's urgent, I need a locksmith") returns urgency: "emergency"
    - extractIntent("ASAP need electrician downtown") returns urgency: "emergency"
    - getDisambiguationPrompt('en') returns string containing "plumbing, electrical, cleaning"
    - getDisambiguationPrompt('fr') returns string containing "plomberie"
    - isIntentComplete({ serviceType: "plumber", location: "Austin" }) returns true
    - isIntentComplete({ serviceType: "plumber" }) returns false
    - isIntentComplete({}) returns false
  </behavior>
  <action>
    **Write tests first (RED), then implement (GREEN).**

    Create `tests/lib/ai/intent-extractor.test.ts`:
    - Import `{ extractIntent, getDisambiguationPrompt, isIntentComplete, IntentResult }` from `../../../src/lib/ai/intent-extractor.js`
    - Test suite per behavior list above

    Run tests — all FAIL (RED).

    Create `src/lib/ai/intent-extractor.ts`:

    ```typescript
    export interface IntentResult {
      serviceType: string | undefined;
      location: string | undefined;
      urgency: 'normal' | 'emergency';
      isComplete: boolean;
    }
    ```

    Implement `extractIntent(transcript: string): IntentResult`:
    - Use regex/keyword patterns to extract service type and location from natural speech
    - Service type patterns: match common service keywords (plumber/plombier, electrician/electricien, locksmith/serrurier, cleaner/nettoyeur, HVAC, roofer, painter, etc.)
    - Location patterns: match "in {location}", "near {location}", "a {location}" (French), zip codes (/\b\d{5}\b/), city names after prepositions
    - Urgency detection: if transcript contains "emergency", "urgent", "ASAP", "right now", "urgence", "immediatement" (case-insensitive), set urgency to "emergency"; else "normal"
    - `isComplete` = serviceType !== undefined && location !== undefined

    Implement `isIntentComplete(intent: Partial<{ serviceType: string; location: string }>): boolean`:
    - Returns true if both serviceType and location are defined and non-empty strings

    Implement `getDisambiguationPrompt(language: 'en' | 'fr'): string`:
    - EN: "What kind of help do you need — plumbing, electrical, cleaning, or something else?"
    - FR: "De quel type d'aide avez-vous besoin — plomberie, electricite, nettoyage, ou autre chose?"

    Run tests — all PASS (GREEN).
  </action>
  <verify>
    <automated>npx vitest run tests/lib/ai/intent-extractor.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/ai/intent-extractor.ts contains `export interface IntentResult`
    - src/lib/ai/intent-extractor.ts contains `export function extractIntent(`
    - src/lib/ai/intent-extractor.ts contains `export function getDisambiguationPrompt(`
    - src/lib/ai/intent-extractor.ts contains `export function isIntentComplete(`
    - tests/lib/ai/intent-extractor.test.ts contains at least 12 test cases
    - `npx vitest run tests/lib/ai/intent-extractor.test.ts` exits 0
    - extractIntent("I need a plumber in Austin") returns isComplete: true with serviceType and location defined
    - extractIntent("I need help") returns isComplete: false
  </acceptance_criteria>
  <done>Intent extractor parses service type, location, and urgency from natural speech in both English and French. Disambiguation prompt available in both languages. isIntentComplete correctly validates presence of both serviceType and location. All 12+ tests pass.</done>
</task>

</tasks>

<verification>
```bash
npx vitest run tests/lib/ai/ --reporter=verbose
```
All AI module tests pass (existing + new). No regressions:
```bash
npx vitest run
```
</verification>

<success_criteria>
- Murphy prompt updated: 1-question limit, bilingual rules, service-first greeting, 10-min timeout
- No existing test broken (all 14 original murphy-system tests still pass)
- Intent extractor created with extractIntent, getDisambiguationPrompt, isIntentComplete
- Intent extraction handles both EN and FR natural utterances
- Urgency detection works for emergency/urgent/ASAP keywords
- All tests pass (exit code 0)
</success_criteria>

<output>
After completion, create `.planning/phases/02-voice-conversation-core/02-02-SUMMARY.md`
</output>
