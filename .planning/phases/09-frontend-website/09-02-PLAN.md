---
phase: 09-frontend-website
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - frontend/src/app/page.tsx
  - frontend/src/components/landing/navbar.tsx
  - frontend/src/components/landing/hero-section.tsx
  - frontend/src/components/landing/features-section.tsx
  - frontend/src/components/landing/social-proof-section.tsx
  - frontend/src/components/landing/voice-wave.tsx
  - frontend/src/components/landing/footer.tsx
autonomous: false
requirements: [WEB-01, WEB-07]

must_haves:
  truths:
    - "Visiting / shows a dark-themed landing page with hero headline 'One call replaces five.'"
    - "Landing page displays phone number +1-888-830-6873 as clickable tel: link"
    - "Landing page has features section explaining Call -> AI Finds -> Live Transfer"
    - "Landing page has social proof / testimonials section"
    - "Landing page has navigation bar with Sign In link"
    - "All landing page sections are responsive (mobile through desktop)"
    - "VoiceWave animation respects prefers-reduced-motion"
  artifacts:
    - path: "frontend/src/app/page.tsx"
      provides: "Landing page composing all sections"
      contains: "HeroSection"
    - path: "frontend/src/components/landing/hero-section.tsx"
      provides: "Hero with display headline, sub-copy, CTA, voice wave"
      contains: "One call replaces five"
    - path: "frontend/src/components/landing/features-section.tsx"
      provides: "Three-column feature grid"
      contains: "features"
    - path: "frontend/src/components/landing/navbar.tsx"
      provides: "Navigation bar with logo and Sign In"
      contains: "Sign In"
    - path: "frontend/src/components/landing/voice-wave.tsx"
      provides: "Animated SVG audio waveform"
      contains: "prefers-reduced-motion"
  key_links:
    - from: "frontend/src/app/page.tsx"
      to: "frontend/src/components/landing/hero-section.tsx"
      via: "component import"
      pattern: "import.*HeroSection"
    - from: "frontend/src/components/landing/navbar.tsx"
      to: "/login"
      via: "Next.js Link"
      pattern: "href.*login"
---

<objective>
Build the complete landing page at murphy.help with hero section, features, social proof, navigation, and footer — all following the UI-SPEC.md brand design contract.

Purpose: This is the public face of Murphy. It must communicate the value proposition ("One call replaces five"), show how the service works, provide social proof, and drive callers to the phone number.
Output: A polished, responsive, dark-themed landing page at /.
</objective>

<execution_context>
@/Users/pmc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pmc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-frontend-website/09-CONTEXT.md
@.planning/phases/09-frontend-website/09-UI-SPEC.md
@.planning/phases/09-frontend-website/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build landing page components (NavBar, HeroSection, VoiceWave, FeaturesSection, SocialProofSection, Footer)</name>
  <files>
    frontend/src/components/landing/navbar.tsx,
    frontend/src/components/landing/hero-section.tsx,
    frontend/src/components/landing/voice-wave.tsx,
    frontend/src/components/landing/features-section.tsx,
    frontend/src/components/landing/social-proof-section.tsx,
    frontend/src/components/landing/footer.tsx
  </files>
  <read_first>
    - .planning/phases/09-frontend-website/09-UI-SPEC.md (full design contract — colors, fonts, spacing, copywriting, interactions)
    - .planning/phases/09-frontend-website/09-CONTEXT.md (locked decisions, phone number, domain)
    - frontend/src/app/globals.css (verify brand colors are available)
    - frontend/src/app/layout.tsx (verify fonts are configured)
    - frontend/src/components/ui/button.tsx (shadcn button for CTA)
  </read_first>
  <action>
Create each component following the UI-SPEC.md design contract exactly:

**NavBar** (`navbar.tsx`):
- Fixed top nav, dark background (`bg-background/80 backdrop-blur-sm border-b border-border`)
- Left: "Murphy" text logo in Montserrat Bold or logo image if available
- Right: Nav links (hidden on mobile), "Sign In" button using shadcn Button ghost variant with teal text
- Sign In links to `/login` using Next.js `Link`
- Container: `max-w-7xl mx-auto px-4`
- Mobile: hamburger menu using shadcn Sheet component

**HeroSection** (`hero-section.tsx`):
- Server component
- Centered layout, `py-16` top/bottom (3xl spacing per UI-SPEC)
- Headline: "One call replaces five." in Cormorant Garamond Bold 60px (`font-display text-6xl font-bold leading-[1.1]`)
- Sub-headline: "Murphy finds local service providers, calls them to check availability, then connects you live — all while you wait on the line." in Montserrat Regular 16px, `max-w-[640px] mx-auto text-muted`
- VoiceWave component below sub-headline
- CTA button: "Call Murphy Now" — Azure Teal bg, near-black text, `rounded-full px-8 py-4 text-lg font-bold uppercase`, wrapped in `<a href="tel:+18888306873">`
- Below CTA: "+1-888-830-6873" in Montserrat Regular 14px, muted color
- CTA hover: `hover:-translate-y-0.5 hover:shadow-lg transition-all duration-150` with `motion-safe:` prefix

**VoiceWave** (`voice-wave.tsx`):
- Client component ('use client')
- Animated SVG with 5-7 vertical bars of varying height in Azure Teal (#00BFFF / primary color)
- CSS keyframe animation with 2-second loop, each bar offset for wave effect
- Must include `@media (prefers-reduced-motion: reduce)` to disable animation
- Width: `w-48 h-12` centered
- Use inline `<style>` tag or Tailwind animation utilities

**FeaturesSection** (`features-section.tsx`):
- Server component
- Three-column grid at xl, two at md, one on mobile (`grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-8`)
- Three feature cards using glassmorphism style per UI-SPEC:
  ```css
  backdrop-filter: blur(12px);
  background: oklch(0.13 0 0 / 0.75);
  border: 1px solid oklch(0.30 0 0 / 0.5);
  border-radius: 12px;
  ```
- Card 1: Icon (Phone) + "Call Murphy" + "Describe what you need — a plumber, an electrician, a locksmith. Murphy listens and understands."
- Card 2: Icon (Search) + "AI Finds Providers" + "Murphy searches local providers, checks ratings and availability, then calls them on your behalf."
- Card 3: Icon (Zap/Connect) + "Live Connection" + "Once a provider is available, Murphy connects you live — no hold music, no callbacks."
- Icons from lucide-react: `Phone`, `Search`, `Zap`
- Section heading: "How It Works" in Montserrat Bold 28px

**SocialProofSection** (`social-proof-section.tsx`):
- Server component
- Section heading: "What People Are Saying" in Montserrat Bold 28px
- 3 testimonial cards (placeholder content — can be replaced later):
  - "I needed a plumber at 10pm on a Sunday. Murphy found one in 3 minutes." — Sarah K., Austin TX
  - "No more calling five different contractors. One call to Murphy and I was connected." — James R., Denver CO
  - "The AI actually called providers and checked if they were available. Incredible." — Maria L., Portland OR
- Cards use solid `bg-card` background with `border border-border rounded-xl p-6`
- Each card shows quote text, name, and location
- Grid: 3 columns at lg, 1 on mobile

**Footer** (`footer.tsx`):
- Server component
- Dark background, `border-t border-border py-12`
- Left: "Murphy" logo/text + tagline "One call replaces five." in Montserrat Regular 14px muted
- Center: Navigation links (Dashboard, Missions, Settings) — same as nav
- Right: BuyMeACoffee link (`href={process.env.NEXT_PUBLIC_BUYMEACOFFEE_URL || 'https://buymeacoffee.com'}`)
- Bottom: copyright line
  </action>
  <verify>
    <automated>cd /Users/pmc/Documents/DEV/OpenClaw-Vercel/frontend && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/landing/hero-section.tsx contains "One call replaces five." and "tel:+18888306873" and "Call Murphy Now"
    - frontend/src/components/landing/hero-section.tsx contains "font-display" (Cormorant Garamond usage)
    - frontend/src/components/landing/navbar.tsx contains "Sign In" and "/login"
    - frontend/src/components/landing/voice-wave.tsx contains "use client" and "prefers-reduced-motion"
    - frontend/src/components/landing/features-section.tsx contains "How It Works" and "backdrop-filter" or "backdrop-blur"
    - frontend/src/components/landing/social-proof-section.tsx contains "What People Are Saying"
    - frontend/src/components/landing/footer.tsx contains "One call replaces five." and "buymeacoffee"
    - All 6 component files exist in frontend/src/components/landing/
    - `cd frontend && npx next build` succeeds
  </acceptance_criteria>
  <done>All 6 landing page components built following UI-SPEC design contract. Hero has display headline, CTA with phone number, voice wave animation. Features section explains the 3-step flow. Social proof has testimonials. NavBar has Sign In link. Footer has tagline and BuyMeACoffee link.</done>
</task>

<task type="auto">
  <name>Task 2: Compose landing page and verify responsive layout</name>
  <files>
    frontend/src/app/page.tsx
  </files>
  <read_first>
    - frontend/src/components/landing/navbar.tsx
    - frontend/src/components/landing/hero-section.tsx
    - frontend/src/components/landing/features-section.tsx
    - frontend/src/components/landing/social-proof-section.tsx
    - frontend/src/components/landing/footer.tsx
    - .planning/phases/09-frontend-website/09-UI-SPEC.md (page inventory, responsive breakpoints)
  </read_first>
  <action>
Replace `frontend/src/app/page.tsx` with the full landing page composition:

```tsx
import { NavBar } from '@/components/landing/navbar'
import { HeroSection } from '@/components/landing/hero-section'
import { FeaturesSection } from '@/components/landing/features-section'
import { SocialProofSection } from '@/components/landing/social-proof-section'
import { Footer } from '@/components/landing/footer'

export default function LandingPage() {
  return (
    <>
      <NavBar />
      <main className="min-h-screen bg-background">
        <HeroSection />
        <FeaturesSection />
        <SocialProofSection />
      </main>
      <Footer />
    </>
  )
}
```

Ensure each section has proper vertical spacing:
- HeroSection: `py-16` or `py-24`
- FeaturesSection: `py-12` with `max-w-7xl mx-auto px-4`
- SocialProofSection: `py-12` with `max-w-7xl mx-auto px-4`
- All sections stack vertically with no horizontal overflow
  </action>
  <verify>
    <automated>cd /Users/pmc/Documents/DEV/OpenClaw-Vercel/frontend && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/app/page.tsx imports NavBar, HeroSection, FeaturesSection, SocialProofSection, Footer
    - frontend/src/app/page.tsx contains "min-h-screen" and "bg-background"
    - `cd frontend && npx next build` succeeds
  </acceptance_criteria>
  <done>Landing page composed with all sections in correct order. Page builds and renders with dark theme, hero, features, social proof, and footer.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of landing page</name>
  <files>frontend/src/app/page.tsx</files>
  <action>Human visual verification of the landing page. No code changes — this is a review checkpoint.</action>
  <verify>Human confirms landing page visual quality matches UI-SPEC design contract</verify>
  <done>Human approves the landing page visual design and responsiveness</done>
  <what-built>Complete landing page at / with dark theme, hero ("One call replaces five."), voice wave animation, features section, social proof testimonials, navigation bar, and footer</what-built>
  <how-to-verify>
    1. Run `cd frontend && npm run dev` (starts on localhost:3000)
    2. Open http://localhost:3000 in browser
    3. Verify dark background (#0D0D0D) with near-white text
    4. Verify hero headline "One call replaces five." in serif font (Cormorant Garamond)
    5. Verify "Call Murphy Now" CTA button in Azure Teal with tel: link
    6. Verify phone number +1-888-830-6873 displayed below CTA
    7. Verify voice wave animation (5-7 bars pulsing in teal)
    8. Verify "How It Works" section with 3 feature cards (glassmorphism effect)
    9. Verify "What People Are Saying" section with 3 testimonial cards
    10. Verify "Sign In" button in nav links to /login
    11. Resize to mobile width (~375px) — verify single-column layout, no horizontal scroll
    12. Check footer shows "One call replaces five." tagline
  </how-to-verify>
  <resume-signal>Type "approved" or describe visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Landing page builds without errors
- All sections render with correct dark theme
- Phone number is clickable (tel: link)
- Responsive at mobile, tablet, desktop breakpoints
- VoiceWave respects prefers-reduced-motion
</verification>

<success_criteria>
- Visiting / shows a polished dark-themed landing page
- Hero headline uses Cormorant Garamond serif font
- CTA links to tel:+18888306873
- Features section explains the 3-step Murphy flow
- Social proof section has testimonials
- Page is responsive (no horizontal scroll on mobile)
- Human approves visual quality
</success_criteria>

<output>
After completion, create `.planning/phases/09-frontend-website/09-02-SUMMARY.md`
</output>
