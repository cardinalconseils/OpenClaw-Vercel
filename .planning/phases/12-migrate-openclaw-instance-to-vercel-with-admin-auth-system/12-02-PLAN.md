---
phase: 12-migrate-openclaw-instance-to-vercel-with-admin-auth-system
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/layout.tsx
  - src/app/(dashboard)/dashboard/page.tsx
  - src/app/(dashboard)/missions/page.tsx
  - src/app/(dashboard)/analytics/page.tsx
  - src/app/(dashboard)/settings/page.tsx
  - src/components/dashboard/dashboard-shell.tsx
  - src/components/dashboard/call-history-table.tsx
  - src/components/dashboard/missions-table.tsx
  - src/components/dashboard/service-type-chart.tsx
  - src/components/dashboard/status-badge.tsx
  - src/components/dashboard/account-management.tsx
  - src/components/dashboard/notification-preferences.tsx
  - src/components/dashboard/profile-form.tsx
  - src/components/landing/navbar.tsx
  - src/components/landing/footer.tsx
  - src/app/(auth)/callback/route.ts
  - vercel.json
autonomous: true
requirements:
  - MIGRATE-01
  - MIGRATE-05
  - MIGRATE-06

must_haves:
  truths:
    - "/dashboard, /missions, /analytics, /settings routes no longer exist"
    - "Dashboard components directory is removed"
    - "Navbar shows only Sign In link (no Dashboard/Missions/Analytics)"
    - "Footer shows only legal links and BuyMeACoffee (no Dashboard/Missions/Settings)"
    - "Auth callback redirects to / instead of /dashboard"
    - "vercel.json is removed"
  artifacts:
    - path: "src/components/landing/navbar.tsx"
      provides: "Cleaned navbar without dashboard links"
      contains: "Sign In"
    - path: "src/components/landing/footer.tsx"
      provides: "Cleaned footer without dashboard links"
      contains: "Privacy Policy"
  key_links:
    - from: "src/app/(auth)/callback/route.ts"
      to: "/"
      via: "redirect after auth code exchange"
      pattern: "next.*'/' "
---

<objective>
Remove all dashboard routes, components, and references. Clean up navigation to remove dashboard links. Delete vercel.json. Update auth callback redirect.

Purpose: The dashboard contained fake/placeholder data and is being replaced by OpenClaw's built-in Control UI at /admin. This plan removes the dead code and Vercel-specific configuration.
Output: Clean codebase with no dashboard remnants, no vercel.json, nav links pointing to correct destinations.
</objective>

<execution_context>
@/Users/pmc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pmc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-migrate-openclaw-instance-to-vercel-with-admin-auth-system/12-CONTEXT.md

<interfaces>
<!-- Current navbar navLinks array -->
From src/components/landing/navbar.tsx:
```typescript
const navLinks = [
  { href: '/dashboard', label: 'Dashboard' },
  { href: '/missions', label: 'Missions' },
  { href: '/analytics', label: 'Analytics' },
]
```

<!-- Current footer navLinks array -->
From src/components/landing/footer.tsx:
```typescript
const navLinks = [
  { href: '/dashboard', label: 'Dashboard' },
  { href: '/missions', label: 'Missions' },
  { href: '/settings', label: 'Settings' },
]
```

<!-- Current auth callback default redirect -->
From src/app/(auth)/callback/route.ts:
```typescript
const next = searchParams.get('next') ?? '/dashboard'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dashboard route group and components</name>
  <files>
    src/app/(dashboard)/ (entire directory — DELETE)
    src/components/dashboard/ (entire directory — DELETE)
    vercel.json (DELETE)
  </files>
  <read_first>
    - src/app/(dashboard)/layout.tsx (confirm what's being removed)
    - src/components/dashboard/dashboard-shell.tsx (confirm no other imports)
    - vercel.json (confirm it's Vercel-specific)
  </read_first>
  <action>
    Delete the following directories and files entirely:

    1. `rm -rf src/app/(dashboard)/` — removes:
       - `src/app/(dashboard)/layout.tsx`
       - `src/app/(dashboard)/dashboard/page.tsx`
       - `src/app/(dashboard)/missions/page.tsx`
       - `src/app/(dashboard)/analytics/page.tsx`
       - `src/app/(dashboard)/settings/page.tsx`

    2. `rm -rf src/components/dashboard/` — removes:
       - `dashboard-shell.tsx`
       - `call-history-table.tsx`
       - `missions-table.tsx`
       - `service-type-chart.tsx`
       - `status-badge.tsx`
       - `account-management.tsx`
       - `notification-preferences.tsx`
       - `profile-form.tsx`

    3. `rm vercel.json` — removes Vercel dual-build configuration (Railway does not use this file)

    Before deleting, verify no other files import from these paths:
    ```bash
    grep -r "components/dashboard" src/ --include="*.ts" --include="*.tsx" | grep -v "(dashboard)"
    grep -r "(dashboard)" src/ --include="*.ts" --include="*.tsx" | grep -v "src/app/(dashboard)"
    ```
    If any external imports exist, they must be removed too.
  </action>
  <verify>
    <automated>cd /Users/pmc/Documents/DEV/OpenClaw-Vercel/frontend && test ! -d src/app/\(dashboard\) && test ! -d src/components/dashboard && test ! -f vercel.json && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Directory src/app/(dashboard)/ does not exist
    - Directory src/components/dashboard/ does not exist
    - File vercel.json does not exist
    - `grep -r "components/dashboard" src/ --include="*.ts" --include="*.tsx"` returns no results
  </acceptance_criteria>
  <done>Dashboard route group, dashboard components, and vercel.json are completely removed</done>
</task>

<task type="auto">
  <name>Task 2: Update navbar, footer, and auth callback references</name>
  <files>src/components/landing/navbar.tsx, src/components/landing/footer.tsx, src/app/(auth)/callback/route.ts</files>
  <read_first>
    - src/components/landing/navbar.tsx (current navLinks array)
    - src/components/landing/footer.tsx (current navLinks array)
    - src/app/(auth)/callback/route.ts (current default redirect)
  </read_first>
  <action>
    1. **navbar.tsx** — Remove the `navLinks` array entirely (Dashboard, Missions, Analytics links are dead). Keep only the Sign In link. The navbar should show:
       - Logo "Murphy" (left, links to /)
       - "Sign In" link (right, links to /login)
       - No other nav links

       Replace lines 15-19:
       ```typescript
       // DELETE: const navLinks = [...]
       ```
       Remove the `navLinks.map(...)` JSX blocks in both desktop and mobile sections. Keep the Sign In link. Keep the mobile Sheet but only with Sign In inside it.

    2. **footer.tsx** — Remove the `navLinks` array and the Navigation column entirely. The footer should have:
       - Brand column ("Murphy" + tagline)
       - BuyMeACoffee column
       - Copyright row with Privacy Policy and Terms links
       - NO Dashboard/Missions/Settings links

       Replace lines 3-7:
       ```typescript
       // DELETE: const navLinks = [...]
       ```
       Remove the entire `<nav>` section (lines 29-39). Change grid from `md:grid-cols-3` to `md:grid-cols-2`.

    3. **callback/route.ts** — Change the default redirect from `/dashboard` to `/`:
       ```typescript
       // Line 8: Change from:
       const next = searchParams.get('next') ?? '/dashboard'
       // To:
       const next = searchParams.get('next') ?? '/'
       ```
  </action>
  <verify>
    <automated>cd /Users/pmc/Documents/DEV/OpenClaw-Vercel/frontend && npm test -- --run && grep -c "dashboard" src/components/landing/navbar.tsx src/components/landing/footer.tsx src/app/\(auth\)/callback/route.ts | grep -v ":0$" | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - navbar.tsx does NOT contain the string '/dashboard'
    - navbar.tsx does NOT contain the string '/missions'
    - navbar.tsx does NOT contain the string '/analytics'
    - footer.tsx does NOT contain the string '/dashboard'
    - footer.tsx does NOT contain the string '/missions'
    - footer.tsx does NOT contain the string '/settings' (the nav link, not a generic word)
    - footer.tsx contains `md:grid-cols-2` (not md:grid-cols-3)
    - callback/route.ts contains `?? '/'` (not `?? '/dashboard'`)
    - `npm test -- --run` exits 0
  </acceptance_criteria>
  <done>All navigation references to /dashboard removed; auth callback redirects to /; tests pass</done>
</task>

</tasks>

<verification>
- `cd /Users/pmc/Documents/DEV/OpenClaw-Vercel/frontend && npm test -- --run` — all tests pass
- `grep -rn "dashboard" src/ --include="*.ts" --include="*.tsx"` — no results (dashboard fully removed)
- `test ! -f vercel.json` — vercel.json removed
</verification>

<success_criteria>
- Zero references to /dashboard, /missions, /analytics, /settings routes in any source file
- Dashboard components directory does not exist
- vercel.json does not exist
- Auth callback redirects to /
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/12-migrate-openclaw-instance-to-vercel-with-admin-auth-system/12-02-SUMMARY.md`
</output>
