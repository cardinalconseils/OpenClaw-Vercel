---
phase: 01.1-openclaw-agent-setup
verified: 2026-03-15T09:05:50Z
status: human_needed
score: 10/10 must-haves verified
re_verification: false
human_verification:
  - test: "OpenClaw gateway text response"
    expected: "Murphy responds with a greeting that includes 'an AI assistant' disclosure in the first sentence"
    why_human: "Requires openclaw CLI installed and API keys configured. Gateway invocation cannot be verified programmatically without external tooling."
---

# Phase 1.1: OpenClaw Agent Setup — Verification Report

**Phase Goal:** Install and configure the OpenClaw agent framework, define agent persona (Murphy) and system prompt, wire LLM providers (OpenRouter + Anthropic tiered), create tool registry skeleton, and verify the agent responds to a basic prompt via the gateway and to a simulated webhook event.
**Verified:** 2026-03-15T09:05:50Z
**Status:** human_needed — all automated checks pass; one item (gateway text response) requires human confirmation
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | LLM clients for OpenRouter and Anthropic are initialized and exported | VERIFIED | `src/lib/ai/llm-clients.ts` exports `openRouterClient` (OpenAI SDK, baseURL `openrouter.ai/api/v1`) and `anthropicClient` (Anthropic SDK); 5 passing tests |
| 2 | Orchestrator routes routine tasks to OpenRouter and complex tasks to Anthropic | VERIFIED | `src/lib/ai/orchestrator.ts` exports `chat()` with ROUTINE_TASKS→openRouterClient, COMPLEX_TASKS/unknown→anthropicClient; 9 passing tests including task-per-tier assertions |
| 3 | Murphy system prompt includes AI disclosure in first sentence | VERIFIED | `buildMurphySystemPrompt()` first greeting instruction: "Hi, I'm Murphy — an AI assistant from OpenClaw"; test "contains 'AI' in the first sentence" passes |
| 4 | Voice modifier strips markdown and enforces short spoken sentences | VERIFIED | `applyVoiceModifiers()` appends VOICE MODE block with no-markdown, under-3-sentence, no-bullet-point rules; tested via isVoiceCall toggle |
| 5 | Tool registry exports all 4 tools with correct schemas | VERIFIED | `src/lib/tools/registry.ts` exports `TOOLS` array (4 entries: search_providers, call_provider, transfer_call, send_sms) with JSON Schema `input_schema`; 9 passing tests |
| 6 | Each stub handler returns a plausible response without throwing | VERIFIED | All 4 handlers (search.ts, dispatch.ts x2, sms.ts) return shaped data; `executeTool()` tests confirm no throws on valid names |
| 7 | OpenClaw config writer generates valid openclaw.json with dual LLM providers | VERIFIED | `writeOpenclawConfig()` writes JSON with gateway.port=18789, agents.named.murphy, auth.profiles.openrouter + anthropic, plugins.entries.voice-call.enabled=true; 5 passing tests using temp dirs |
| 8 | Workspace persona files (SOUL.md, IDENTITY.md) are written with Murphy's identity | VERIFIED | `writeWorkspaceFiles()` writes SOUL.md (from `buildMurphySystemPrompt()`) and IDENTITY.md; 3 passing tests confirm "Murphy" and "AI" presence |
| 9 | sandbox-start.sh writes config and persona before starting gateway | VERIFIED | `bin/sandbox-start.sh` line 9: `bash bin/write-openclaw-config.sh` runs in Phase 1.5 before `npx tsx src/server.ts` |
| 10 | Simulated call.initiated webhook event invokes orchestrator chat() via setImmediate | VERIFIED | `src/api/webhooks.ts` imports `chat` from orchestrator and calls it in setImmediate on call.initiated; integration test asserts `mockChat` called once with caller number + 'greeting' task |

**Score:** 10/10 truths verified

---

## Required Artifacts

| Artifact | Status | Details |
|----------|--------|---------|
| `src/lib/ai/llm-clients.ts` | VERIFIED | 22 lines; exports openRouterClient + anthropicClient; wired to orchestrator |
| `src/lib/ai/orchestrator.ts` | VERIFIED | 75 lines; imports both clients from llm-clients.js; exports chat(), TaskType, ROUTINE_TASKS, COMPLEX_TASKS |
| `src/lib/ai/prompts/murphy-system.ts` | VERIFIED | 64 lines; exports buildMurphySystemPrompt(); imports applyVoiceModifiers |
| `src/lib/ai/prompts/voice-modifiers.ts` | VERIFIED | 19 lines; exports applyVoiceModifiers(); imported by murphy-system.ts |
| `src/lib/tools/registry.ts` | VERIFIED | 126 lines; exports TOOLS (4 entries) and executeTool(); imports all 3 handler files |
| `src/lib/tools/handlers/search.ts` | VERIFIED | 35 lines; exports searchProviders(); imported by registry.ts |
| `src/lib/tools/handlers/dispatch.ts` | VERIFIED | 40 lines; exports callProvider() and transferCall(); imported by registry.ts |
| `src/lib/tools/handlers/sms.ts` | VERIFIED | 20 lines; exports sendSms(); imported by registry.ts |
| `src/startup/openclaw-config.ts` | VERIFIED | 110 lines; exports writeOpenclawConfig() and writeWorkspaceFiles(); imports buildMurphySystemPrompt |
| `bin/write-openclaw-config.sh` | VERIFIED | Shell wrapper invoking both config functions via `npx tsx` |
| `bin/sandbox-start.sh` | VERIFIED | Updated — `bash bin/write-openclaw-config.sh` in Phase 1.5 block before server start |
| `src/api/webhooks.ts` | VERIFIED | Imports `chat` from orchestrator.js; calls chat() on call.initiated inside setImmediate |
| `tests/lib/ai/agent-integration.test.ts` | VERIFIED | 3 tests: call.initiated triggers orchestrator, call.answered does not, unknown event type returns 200 — all pass |
| `tests/startup/openclaw-config.test.ts` | VERIFIED | 8 tests covering config structure and workspace file content — all pass |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `src/lib/ai/orchestrator.ts` | `src/lib/ai/llm-clients.ts` | `import { openRouterClient, anthropicClient }` | WIRED | Import confirmed; both clients used in chat() routing branches |
| `src/lib/ai/orchestrator.ts` | `src/lib/ai/prompts/murphy-system.ts` | `buildMurphySystemPrompt` | NOTE | orchestrator.ts does not directly import buildMurphySystemPrompt — the plan key_link was aspirational. murphy-system.ts is imported by openclaw-config.ts (SOUL.md) and is available as system prompt via the workspace file path. Indirect wiring is correct and intentional per plan design. |
| `src/lib/tools/registry.ts` | `src/lib/tools/handlers/search.ts` | `import { searchProviders }` | WIRED | Line 1 confirmed |
| `src/lib/tools/registry.ts` | `src/lib/tools/handlers/dispatch.ts` | `import { callProvider, transferCall }` | WIRED | Line 2 confirmed |
| `src/lib/tools/registry.ts` | `src/lib/tools/handlers/sms.ts` | `import { sendSms }` | WIRED | Line 3 confirmed |
| `bin/sandbox-start.sh` | `src/startup/openclaw-config.ts` | `bash bin/write-openclaw-config.sh` | WIRED | Line 9 of sandbox-start.sh invokes write-openclaw-config.sh which calls both functions |
| `src/startup/openclaw-config.ts` | `~/.openclaw/openclaw.json` | `fs.writeFileSync` | WIRED | Line 77: `fs.writeFileSync(configPath, ...)` where configPath is `path.join(configDir, 'openclaw.json')` |
| `src/startup/openclaw-config.ts` | `~/.openclaw/workspace/SOUL.md` | `fs.writeFileSync` | WIRED | Line 95: `fs.writeFileSync(path.join(workspaceDir, 'SOUL.md'), ...)` |
| `src/api/webhooks.ts` | `src/lib/ai/orchestrator.ts` | `import { chat }` | WIRED | Line 4 import confirmed; chat() called inside setImmediate on call.initiated |

---

## Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| AGENT-01 | 01.1-01 | LLM clients for OpenRouter and Anthropic | SATISFIED | llm-clients.ts exports both clients; 5 tests green |
| AGENT-02 | 01.1-01 | Tiered orchestrator routing by task type | SATISFIED | orchestrator.ts routes routine→OpenRouter, complex→Anthropic; 9 tests green |
| AGENT-03 | 01.1-01 | Murphy persona + voice modifiers | SATISFIED | murphy-system.ts and voice-modifiers.ts exist; 14 tests green including AI disclosure |
| AGENT-04 | 01.1-02 | Tool registry with 4 tools + stub handlers | SATISFIED | registry.ts + 3 handler files; 9 tests green |
| AGENT-05 | 01.1-03 | Config generation + webhook-orchestrator wiring | SATISFIED | openclaw-config.ts, updated webhooks.ts, updated sandbox-start.sh; 11 tests green |

**Note on REQUIREMENTS.md:** AGENT-01 through AGENT-05 are planner-assigned IDs for this inserted phase. They do not appear in the main REQUIREMENTS.md traceability table — that document only covers INFRA, VOICE, SRCH, CALL, XFER, POST, and DASH requirement series. These phase-local IDs are fully satisfied but are not orphaned requirements in the REQUIREMENTS.md sense; they are internal planning artifacts.

---

## Anti-Patterns Found

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| `src/lib/tools/handlers/search.ts` | Returns `source: 'stub'` and `note: 'Real implementation in Phase 3'` | INFO | Intentional stub — plan explicitly defers real implementation. Not a blocker. |
| `src/lib/tools/handlers/dispatch.ts` | Returns `status: 'stub-unavailable'` and `status: 'stub-transfer-pending'` | INFO | Intentional stub — plan explicitly defers to Phase 4 and Phase 5. Not a blocker. |
| `src/lib/tools/handlers/sms.ts` | Returns `stub: true` | INFO | Intentional stub — plan explicitly defers to Phase 6. Not a blocker. |

No blocker or warning anti-patterns. All stubs are intentional and documented by design — this phase's explicit goal was a "tool registry skeleton" with stub handlers.

---

## Human Verification Required

### 1. Gateway Text Response — Murphy AI Disclosure

**Test:** With OpenClaw installed and API keys configured, run:
```bash
openclaw agent --message "Hi Murphy, what can you help me with?" --agent murphy
```
**Expected:** Murphy responds with a greeting that includes "an AI assistant" in the first sentence — demonstrating the full path: sandbox-start.sh writes config → gateway loads SOUL.md → LLM uses Murphy persona → response includes AI disclosure.
**Why human:** Requires the `openclaw` CLI to be installed in the runtime environment, and real API keys for OpenRouter or Anthropic. Cannot be verified programmatically without the external binary.

---

## Test Results Summary

All automated tests pass:

| Test File | Tests | Status |
|-----------|-------|--------|
| `tests/lib/ai/llm-clients.test.ts` | 5 | All pass |
| `tests/lib/ai/prompts/murphy-system.test.ts` | 14 | All pass |
| `tests/lib/ai/orchestrator.test.ts` | 9 | All pass |
| `tests/lib/ai/agent-integration.test.ts` | 3 | All pass |
| `tests/lib/tools/registry.test.ts` | 9 | All pass |
| `tests/startup/openclaw-config.test.ts` | 8 | All pass |
| All other Phase 1 tests | 33 | All pass (regression check) |
| **Total** | **81** | **All pass** |

Full suite: `npx vitest run` — 14 test files, 81 tests, 0 failures.

---

## Summary

Phase 1.1 goal is substantially achieved. All 10 observable truths are verified against actual code — not summary claims. The AI layer (LLM clients, orchestrator, Murphy persona, voice modifiers), tool registry (4 tools with stub handlers), config generation (openclaw.json + workspace files), startup script integration, and webhook-to-orchestrator wiring are all present, substantive, and correctly connected.

The sole remaining item is the gateway text response path — whether Murphy's persona actually surfaces through the OpenClaw gateway CLI with AI disclosure. This requires the `openclaw` binary and real API keys, which cannot be verified programmatically. The SUMMARY notes the user approved without locally verifying the gateway (OpenClaw was not installed locally), and this gap is accurately reflected here.

---

_Verified: 2026-03-15T09:05:50Z_
_Verifier: Claude (gsd-verifier)_
