---
phase: 01.1-openclaw-agent-setup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/tools/registry.ts
  - src/lib/tools/handlers/search.ts
  - src/lib/tools/handlers/dispatch.ts
  - src/lib/tools/handlers/sms.ts
  - tests/lib/tools/registry.test.ts
autonomous: true
requirements:
  - AGENT-04

must_haves:
  truths:
    - "Tool registry exports all 4 tools with correct schemas"
    - "Each stub handler returns a plausible response without throwing"
    - "Tool definitions align with dispatch pipeline stages"
  artifacts:
    - path: "src/lib/tools/registry.ts"
      provides: "Tool definitions array and dispatch function"
      exports: ["TOOLS", "executeTool"]
    - path: "src/lib/tools/handlers/search.ts"
      provides: "Stub search_providers handler"
      exports: ["searchProviders"]
    - path: "src/lib/tools/handlers/dispatch.ts"
      provides: "Stub call_provider and transfer_call handlers"
      exports: ["callProvider", "transferCall"]
    - path: "src/lib/tools/handlers/sms.ts"
      provides: "Stub send_sms handler"
      exports: ["sendSms"]
  key_links:
    - from: "src/lib/tools/registry.ts"
      to: "src/lib/tools/handlers/search.ts"
      via: "import searchProviders"
      pattern: "import.*from.*handlers/search"
    - from: "src/lib/tools/registry.ts"
      to: "src/lib/tools/handlers/dispatch.ts"
      via: "import callProvider, transferCall"
      pattern: "import.*from.*handlers/dispatch"
    - from: "src/lib/tools/registry.ts"
      to: "src/lib/tools/handlers/sms.ts"
      via: "import sendSms"
      pattern: "import.*from.*handlers/sms"
---

<objective>
Create the tool registry skeleton with stub handlers for all 4 tools (search_providers, call_provider, transfer_call, send_sms). Each tool has a complete schema definition and a stub handler that returns plausible mock data.

Purpose: The agent needs tool definitions to function — even as stubs, these allow Murphy to reason about capabilities and produce tool calls. Later phases replace stubs with real implementations. The registry pattern established here carries through the entire project.

Output: Registry file with tool definitions + dispatch, 3 handler files with stubs, 1 test file.
</objective>

<execution_context>
@/Users/pmc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pmc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01.1-openclaw-agent-setup/01.1-CONTEXT.md
@.planning/phases/01.1-openclaw-agent-setup/01.1-RESEARCH.md

@.claude/skills/dispatch-process/SKILL.md

<interfaces>
<!-- Dispatch pipeline from skill definition — tools must align with these stages -->
Pipeline: INTAKE -> SEARCH -> RANK -> DIAL -> CONNECT -> FOLLOW-UP

Tool-to-stage mapping:
- search_providers -> SEARCH stage
- call_provider -> DIAL stage
- transfer_call -> CONNECT stage
- send_sms -> FOLLOW-UP stage
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tool registry and stub handlers</name>
  <files>
    src/lib/tools/registry.ts,
    src/lib/tools/handlers/search.ts,
    src/lib/tools/handlers/dispatch.ts,
    src/lib/tools/handlers/sms.ts,
    tests/lib/tools/registry.test.ts
  </files>
  <behavior>
    - registry.test.ts: TOOLS array has exactly 4 entries
    - registry.test.ts: each tool has name, description, and input_schema fields
    - registry.test.ts: tool names are search_providers, call_provider, transfer_call, send_sms
    - registry.test.ts: executeTool('search_providers', { service_type: 'plumber', location: 'Austin' }) resolves without throwing
    - registry.test.ts: executeTool('search_providers', ...) returns object with providers array
    - registry.test.ts: executeTool('call_provider', { phone_number: '+15550001234', provider_name: 'Acme' }) resolves with status field
    - registry.test.ts: executeTool('transfer_call', { provider_phone: '+15550001234' }) resolves with status field
    - registry.test.ts: executeTool('send_sms', { to: '+15550001234', message: 'test' }) resolves with sent: true
    - registry.test.ts: executeTool('nonexistent_tool', {}) throws or returns error
  </behavior>
  <action>
    1. Create `src/lib/tools/handlers/search.ts`:
       - Export `async function searchProviders(params: { service_type: string; location: string; urgency?: string }): Promise<object>`
       - Stub returns: `{ providers: [{ name: 'Stub Provider', phone: '+15550000000', rating: 4.5, distance: '2.3 mi' }], source: 'stub', note: 'Real implementation in Phase 3' }`
       - Log: `[tools:search] STUB — searchProviders("${params.service_type}", "${params.location}")`

    2. Create `src/lib/tools/handlers/dispatch.ts`:
       - Export `async function callProvider(params: { phone_number: string; provider_name: string }): Promise<object>`
       - Stub returns: `{ status: 'stub-unavailable', provider: params.provider_name, note: 'Real implementation in Phase 4' }`
       - Export `async function transferCall(params: { provider_phone: string; caller_context?: string }): Promise<object>`
       - Stub returns: `{ status: 'stub-transfer-pending', note: 'Real implementation in Phase 5' }`
       - Log each call with `[tools:dispatch]` prefix

    3. Create `src/lib/tools/handlers/sms.ts`:
       - Export `async function sendSms(params: { to: string; message: string }): Promise<object>`
       - Stub returns: `{ sent: true, stub: true, note: 'Real implementation in Phase 6' }`
       - Log: `[tools:sms] STUB — sendSms to ${params.to}`

    4. Create `src/lib/tools/registry.ts`:
       - Export `TOOLS` array with 4 tool definitions, each having: name, description, input_schema (JSON Schema format with properties and required fields)
       - Tool schemas match the research examples exactly (search_providers has service_type, location, urgency; call_provider has phone_number, provider_name; transfer_call has provider_phone, caller_context; send_sms has to, message)
       - Export `async function executeTool(name: string, params: Record<string, unknown>): Promise<object>`
       - executeTool dispatches to the correct handler by name, throws Error for unknown tools
       - Log dispatch: `[tools:registry] Executing tool: ${name}`

    All files use strict TypeScript. Handler params should use explicit interfaces, not `any`.
  </action>
  <verify>
    <automated>npx vitest run tests/lib/tools/registry.test.ts</automated>
  </verify>
  <done>
    - TOOLS array has 4 entries with correct schemas
    - All 4 stub handlers return plausible data without throwing
    - executeTool dispatches correctly by name
    - Unknown tool name throws/returns error
    - All tests pass
  </done>
</task>

</tasks>

<verification>
Tool registry tests pass:
```bash
npx vitest run tests/lib/tools/
```
</verification>

<success_criteria>
- 4 tools defined with JSON Schema input definitions
- Each stub handler callable and returns mock data
- executeTool dispatcher routes by name
- Tool names align with dispatch pipeline stages
- All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-openclaw-agent-setup/01.1-02-SUMMARY.md`
</output>
