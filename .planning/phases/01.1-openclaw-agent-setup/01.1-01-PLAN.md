---
phase: 01.1-openclaw-agent-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/ai/llm-clients.ts
  - src/lib/ai/orchestrator.ts
  - src/lib/ai/prompts/murphy-system.ts
  - src/lib/ai/prompts/voice-modifiers.ts
  - tests/lib/ai/llm-clients.test.ts
  - tests/lib/ai/orchestrator.test.ts
  - tests/lib/ai/prompts/murphy-system.test.ts
  - .env.example
autonomous: true
requirements:
  - AGENT-01
  - AGENT-02
  - AGENT-03

must_haves:
  truths:
    - "LLM clients for OpenRouter and Anthropic are initialized and exported"
    - "Orchestrator routes routine tasks to OpenRouter and complex tasks to Anthropic"
    - "Murphy system prompt includes AI disclosure in first sentence"
    - "Voice modifier strips markdown and enforces short spoken sentences"
  artifacts:
    - path: "src/lib/ai/llm-clients.ts"
      provides: "OpenRouter + Anthropic client initialization"
      exports: ["openRouterClient", "anthropicClient"]
    - path: "src/lib/ai/orchestrator.ts"
      provides: "Tiered LLM routing"
      exports: ["chat", "TaskType"]
    - path: "src/lib/ai/prompts/murphy-system.ts"
      provides: "Murphy persona system prompt builder"
      exports: ["buildMurphySystemPrompt"]
    - path: "src/lib/ai/prompts/voice-modifiers.ts"
      provides: "Voice-mode prompt adjustments"
      exports: ["applyVoiceModifiers"]
  key_links:
    - from: "src/lib/ai/orchestrator.ts"
      to: "src/lib/ai/llm-clients.ts"
      via: "import openRouterClient, anthropicClient"
      pattern: "import.*from.*llm-clients"
    - from: "src/lib/ai/orchestrator.ts"
      to: "src/lib/ai/prompts/murphy-system.ts"
      via: "system prompt used in chat messages"
      pattern: "buildMurphySystemPrompt"
---

<objective>
Create the AI layer: dual LLM client initialization (OpenRouter + Anthropic), tiered orchestrator that routes by task type, Murphy's system prompt with AI disclosure compliance, and voice mode modifiers.

Purpose: This is the brain of Murphy — the routing logic that decides which LLM handles each task type, and the persona definition that shapes all responses. Everything downstream (tools, webhook handling, voice) depends on this layer existing.

Output: Four production files in `src/lib/ai/` + three test files + updated `.env.example` with `OPENROUTER_API_KEY`.
</objective>

<execution_context>
@/Users/pmc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pmc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-openclaw-agent-setup/01.1-CONTEXT.md
@.planning/phases/01.1-openclaw-agent-setup/01.1-RESEARCH.md
@.planning/phases/01.1-openclaw-agent-setup/01.1-VALIDATION.md

@src/server.ts
@src/api/webhooks.ts
@.env.example

<interfaces>
<!-- Existing patterns from Phase 1 that this plan must follow -->

From src/server.ts:
- Express v5, no global body parser
- Console logging with [component] prefix pattern
- Exports: app, startServer()

From .env.example:
- ANTHROPIC_API_KEY= (exists)
- OPENROUTER_API_KEY= (MISSING — must add)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LLM clients, system prompt, and voice modifiers</name>
  <files>
    src/lib/ai/llm-clients.ts,
    src/lib/ai/prompts/murphy-system.ts,
    src/lib/ai/prompts/voice-modifiers.ts,
    tests/lib/ai/llm-clients.test.ts,
    tests/lib/ai/prompts/murphy-system.test.ts,
    .env.example
  </files>
  <behavior>
    - llm-clients.test.ts: openRouterClient is an OpenAI instance with baseURL containing "openrouter.ai"
    - llm-clients.test.ts: anthropicClient is an Anthropic instance
    - llm-clients.test.ts: openRouterClient has default headers for HTTP-Referer and X-Title
    - murphy-system.test.ts: buildMurphySystemPrompt() returns string containing "Murphy"
    - murphy-system.test.ts: buildMurphySystemPrompt() returns string containing "AI" in the first sentence (compliance)
    - murphy-system.test.ts: buildMurphySystemPrompt({ isVoiceCall: true }) includes voice mode directive (no markdown)
    - murphy-system.test.ts: buildMurphySystemPrompt() mentions all 4 tool names (search_providers, call_provider, transfer_call, send_sms)
    - murphy-system.test.ts: prompt includes dispatch pipeline stages (INTAKE, SEARCH, RANK, DIAL, CONNECT, FOLLOW-UP)
  </behavior>
  <action>
    1. Install dependencies: `npm install @anthropic-ai/sdk openai`

    2. Create `src/lib/ai/llm-clients.ts`:
       - Export `openRouterClient`: new OpenAI instance with baseURL `https://openrouter.ai/api/v1`, apiKey from `process.env.OPENROUTER_API_KEY`, defaultHeaders with HTTP-Referer and X-Title for OpenClaw
       - Export `anthropicClient`: new Anthropic instance with apiKey from `process.env.ANTHROPIC_API_KEY`
       - Both use `?? ''` for missing env vars (graceful in test environments)

    3. Create `src/lib/ai/prompts/murphy-system.ts`:
       - Export `buildMurphySystemPrompt(context?: { callerPhone?: string; isVoiceCall?: boolean }): string`
       - Persona: Murphy, male, AI phone concierge at OpenClaw Service Matchmaker
       - FIRST SENTENCE of greeting instruction must include "AI" for FCC/CA SB-1001 compliance: "Hi, I'm Murphy — an AI assistant from OpenClaw."
       - Tone: warm, competent, direct — like a knowledgeable friend. No corporate-speak. No "certainly!" or "absolutely!"
       - Include dispatch pipeline stages: INTAKE -> SEARCH -> RANK -> DIAL -> CONNECT -> FOLLOW-UP
       - List all 4 tools: search_providers, call_provider, transfer_call, send_sms
       - Conversation rules: 2-turn clarification max, no dead air, mirror caller language
       - If isVoiceCall: append voice mode directive via applyVoiceModifiers()

    4. Create `src/lib/ai/prompts/voice-modifiers.ts`:
       - Export `applyVoiceModifiers(basePrompt: string): string`
       - Appends voice-specific rules: short spoken sentences, no markdown, no bullet points, no lists, speak naturally, keep responses under 3 sentences unless elaborating

    5. Add `OPENROUTER_API_KEY=` to `.env.example` in the LLM Providers section

    Use the research-provided code examples as reference but implement to match project conventions (kebab-case files, structured logging prefix pattern, strict TypeScript).
  </action>
  <verify>
    <automated>npx vitest run tests/lib/ai/llm-clients.test.ts tests/lib/ai/prompts/murphy-system.test.ts</automated>
  </verify>
  <done>
    - openRouterClient and anthropicClient are exported and correctly configured
    - Murphy system prompt includes AI disclosure in first sentence, all 4 tool names, dispatch pipeline
    - Voice modifier appends spoken-language directives
    - .env.example has OPENROUTER_API_KEY
    - All tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tiered LLM orchestrator</name>
  <files>
    src/lib/ai/orchestrator.ts,
    tests/lib/ai/orchestrator.test.ts
  </files>
  <behavior>
    - orchestrator.test.ts: chat() with task='greeting' calls openRouterClient (not anthropicClient)
    - orchestrator.test.ts: chat() with task='disambiguation' calls anthropicClient (not openRouterClient)
    - orchestrator.test.ts: chat() with task='intent-capture' routes to OpenRouter (routine)
    - orchestrator.test.ts: chat() with task='provider-ranking' routes to Anthropic (complex)
    - orchestrator.test.ts: chat() with task='unknown' routes to Anthropic (complex fallback for safety)
    - orchestrator.test.ts: chat() with task='filler' routes to OpenRouter with max_tokens <= 200
    - orchestrator.test.ts: ROUTINE_TASKS and COMPLEX_TASKS are exported for transparency
  </behavior>
  <action>
    Create `src/lib/ai/orchestrator.ts`:
    - Export `TaskType = 'greeting' | 'intent-capture' | 'filler' | 'status-update' | 'disambiguation' | 'provider-ranking' | 'transfer-logic' | 'unknown'`
    - Export `ROUTINE_TASKS: TaskType[]` = ['greeting', 'intent-capture', 'filler', 'status-update']
    - Export `COMPLEX_TASKS: TaskType[]` = ['disambiguation', 'provider-ranking', 'transfer-logic']
    - Export `async function chat(messages: Array<{ role: string; content: string }>, task: TaskType = 'unknown'): Promise<string>`
    - Routing: if task is in ROUTINE_TASKS, use openRouterClient with model 'google/gemini-2.5-flash-lite', max_tokens 200
    - Otherwise (complex or unknown): use anthropicClient with model 'claude-sonnet-4-5-20251022', max_tokens 500
    - Import clients from './llm-clients.js'
    - Log routing decision: `[orchestrator] Routing "${task}" to ${tier}`

    Tests should mock both clients (vi.mock) to verify routing without real API calls. Assert which client's create method was called for each task type.
  </action>
  <verify>
    <automated>npx vitest run tests/lib/ai/orchestrator.test.ts</automated>
  </verify>
  <done>
    - chat() routes routine tasks to OpenRouter, complex/unknown to Anthropic
    - TaskType and task lists are exported
    - Routing is logged with [orchestrator] prefix
    - All tests pass with mocked providers
  </done>
</task>

</tasks>

<verification>
All AI layer tests pass:
```bash
npx vitest run tests/lib/ai/
```
</verification>

<success_criteria>
- LLM clients initialized for both providers with correct configuration
- Orchestrator routes by task type: routine -> OpenRouter, complex -> Anthropic
- Murphy persona prompt includes AI disclosure, dispatch pipeline, tool list
- Voice modifiers enforce spoken-language constraints
- .env.example updated with OPENROUTER_API_KEY
- All unit tests green
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-openclaw-agent-setup/01.1-01-SUMMARY.md`
</output>
