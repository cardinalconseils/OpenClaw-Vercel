---
phase: 01.1-openclaw-agent-setup
plan: 03
type: execute
wave: 2
depends_on:
  - 01.1-01
  - 01.1-02
files_modified:
  - src/startup/openclaw-config.ts
  - bin/write-openclaw-config.sh
  - bin/sandbox-start.sh
  - src/api/webhooks.ts
  - tests/startup/openclaw-config.test.ts
  - tests/lib/ai/agent-integration.test.ts
autonomous: false
requirements:
  - AGENT-05

must_haves:
  truths:
    - "OpenClaw config writer generates valid openclaw.json with dual LLM providers"
    - "Workspace persona files (SOUL.md, IDENTITY.md) are written with Murphy's identity"
    - "sandbox-start.sh writes config and persona before starting gateway"
    - "Simulated call.initiated webhook event invokes orchestrator chat() via setImmediate"
  artifacts:
    - path: "src/startup/openclaw-config.ts"
      provides: "OpenClaw config and workspace file generation"
      exports: ["writeOpenclawConfig", "writeWorkspaceFiles"]
    - path: "bin/write-openclaw-config.sh"
      provides: "Shell script to invoke config writer"
    - path: "src/api/webhooks.ts"
      provides: "Webhook handler wired to orchestrator for call events"
    - path: "tests/lib/ai/agent-integration.test.ts"
      provides: "Integration test for webhook-to-orchestrator pipeline"
    - path: "tests/startup/openclaw-config.test.ts"
      provides: "Config generation unit tests"
  key_links:
    - from: "bin/sandbox-start.sh"
      to: "src/startup/openclaw-config.ts"
      via: "npx tsx invocation before openclaw start"
      pattern: "write-openclaw-config"
    - from: "src/startup/openclaw-config.ts"
      to: "~/.openclaw/openclaw.json"
      via: "fs.writeFileSync"
      pattern: "writeFileSync.*openclaw\\.json"
    - from: "src/startup/openclaw-config.ts"
      to: "~/.openclaw/workspace/SOUL.md"
      via: "fs.writeFileSync"
      pattern: "writeFileSync.*SOUL\\.md"
    - from: "src/api/webhooks.ts"
      to: "src/lib/ai/orchestrator.ts"
      via: "import chat and call inside setImmediate on call.initiated"
      pattern: "import.*chat.*from.*orchestrator"
---

<objective>
Write the OpenClaw configuration generator (openclaw.json + workspace persona files), update sandbox-start.sh to run config generation before gateway start, wire the webhook handler's setImmediate() block to invoke the orchestrator on call events, and create the integration test that verifies the full webhook-to-orchestrator pipeline.

Purpose: This wires everything together. Plans 01 and 02 created the AI layer and tools — this plan makes them discoverable by the OpenClaw gateway through config files, and proves the webhook-to-agent pipeline works end-to-end by confirming the orchestrator is actually invoked when a call event arrives.

Output: Config generator, updated startup script, updated webhook handler with orchestrator wiring, integration test, human verification of both gateway text and webhook paths.
</objective>

<execution_context>
@/Users/pmc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pmc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01.1-openclaw-agent-setup/01.1-CONTEXT.md
@.planning/phases/01.1-openclaw-agent-setup/01.1-RESEARCH.md
@.planning/phases/01.1-openclaw-agent-setup/01.1-VALIDATION.md

@src/server.ts
@src/api/webhooks.ts
@src/startup/gateway-manager.ts
@bin/sandbox-start.sh

<!-- Plan 01 creates these — executor should read them for interface context -->
@src/lib/ai/llm-clients.ts
@src/lib/ai/orchestrator.ts
@src/lib/ai/prompts/murphy-system.ts

<!-- Plan 02 creates these -->
@src/lib/tools/registry.ts

<interfaces>
<!-- From research: openclaw.json structure (JSON5) -->
Config lives at: ~/.openclaw/openclaw.json
Workspace lives at: ~/.openclaw/workspace/

Key config sections:
- gateway: { bind, port, token }
- agents.named.murphy: { model: { primary, fallbacks }, systemPromptFile }
- auth.profiles: { openrouter: { mode, key }, anthropic: { mode, key } }
- plugins.entries.voice-call: { enabled, config: { provider, fromNumber, telnyx, serve } }

Workspace files:
- SOUL.md — Murphy's personality and rules
- IDENTITY.md — Name, vibe, emoji

<!-- From existing bin/sandbox-start.sh (to be extended) -->
sandbox-start.sh currently:
- Runs npx tsx src/server.ts in background
- Polls /health for 45s
- Updates webhook URL

<!-- From Plan 01 orchestrator (created in Wave 1) -->
From src/lib/ai/orchestrator.ts:
- export async function chat(messages: Array<{ role: string; content: string }>, task: TaskType): Promise<string>
- TaskType includes 'greeting', 'intent-capture', etc.

<!-- From existing src/api/webhooks.ts (to be modified) -->
Current webhooks.ts:
- setImmediate() block logs event type but does NOT call orchestrator
- Must be extended to import and invoke chat() on call.initiated events
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OpenClaw config generator and workspace writer</name>
  <files>
    src/startup/openclaw-config.ts,
    bin/write-openclaw-config.sh,
    bin/sandbox-start.sh,
    tests/startup/openclaw-config.test.ts
  </files>
  <behavior>
    - openclaw-config.test.ts: writeOpenclawConfig() creates ~/.openclaw/openclaw.json (use temp dir)
    - openclaw-config.test.ts: generated config contains gateway.port = 18789
    - openclaw-config.test.ts: generated config contains agents.named.murphy with model.primary containing "openrouter"
    - openclaw-config.test.ts: generated config contains auth.profiles.openrouter and auth.profiles.anthropic
    - openclaw-config.test.ts: generated config contains plugins.entries.voice-call.enabled = true
    - openclaw-config.test.ts: writeWorkspaceFiles() creates SOUL.md containing "Murphy"
    - openclaw-config.test.ts: writeWorkspaceFiles() creates IDENTITY.md containing "Murphy"
    - openclaw-config.test.ts: SOUL.md contains "AI" (disclosure rule)
  </behavior>
  <action>
    1. Create `src/startup/openclaw-config.ts`:
       - Export `function writeOpenclawConfig(options?: { configDir?: string }): void`
         - Default configDir: `path.join(os.homedir(), '.openclaw')`
         - Creates dir if not exists (recursive)
         - Writes `openclaw.json` (plain JSON — JSON5 comments not needed at runtime) with:
           - gateway: { bind: "loopback", port: 18789, token: process.env.OPENCLAW_DEVICE_TOKEN }
           - agents.defaults.model: { primary: "openrouter/google/gemini-2.5-flash-lite", fallbacks: ["anthropic/claude-sonnet-4-5"] }
           - agents.named.murphy: { model: { primary: "openrouter/google/gemini-2.5-flash-lite", fallbacks: ["anthropic/claude-sonnet-4-5"] }, systemPromptFile: "workspace/SOUL.md" }
           - auth.profiles.openrouter: { mode: "api_key", key: process.env.OPENROUTER_API_KEY }
           - auth.profiles.anthropic: { mode: "api_key", key: process.env.ANTHROPIC_API_KEY }
           - plugins.entries["voice-call"]: { enabled: true, config: { provider: "telnyx", fromNumber: process.env.TELNYX_PHONE_NUMBER, inboundPolicy: "open", telnyx: { apiKey: process.env.TELNYX_API_KEY, connectionId: process.env.TELNYX_CONNECTION_ID, publicKey: process.env.TELNYX_PUBLIC_KEY }, serve: { port: 3334, path: "/voice/telnyx" } } }
         - Log: `[openclaw-config] Wrote openclaw.json to ${configDir}`

       - Export `function writeWorkspaceFiles(options?: { workspaceDir?: string }): void`
         - Default workspaceDir: `path.join(os.homedir(), '.openclaw', 'workspace')`
         - Creates dir if not exists
         - Writes SOUL.md with Murphy's full persona (import buildMurphySystemPrompt and use its output as the soul content, OR write a standalone persona markdown — choose whichever is cleaner. The key requirement is that SOUL.md contains Murphy's identity, AI disclosure rule, conversation rules, dispatch pipeline, and tool list.)
         - Writes IDENTITY.md with: Name: Murphy, Vibe: Friendly professional, Emoji: wrench
         - Log: `[openclaw-config] Wrote workspace files to ${workspaceDir}`

    2. Create `bin/write-openclaw-config.sh`:
       - Simple wrapper: `#!/bin/bash` then `npx tsx -e "import { writeOpenclawConfig, writeWorkspaceFiles } from './src/startup/openclaw-config.js'; writeOpenclawConfig(); writeWorkspaceFiles();"`
       - Make executable (chmod +x in action, or note in verify)

    3. Update `bin/sandbox-start.sh`:
       - Read the existing file first
       - Add a step BEFORE the gateway/server start: run `bash bin/write-openclaw-config.sh`
       - Add comment: `# Phase 1.1: Write OpenClaw config and Murphy persona before gateway start`
       - This ensures config exists before `openclaw start` is invoked by GatewayManager

    Tests use a temp directory (os.tmpdir + unique suffix) for configDir and workspaceDir to avoid writing to real ~/.openclaw during tests. Clean up in afterEach.
  </action>
  <verify>
    <automated>npx vitest run tests/startup/openclaw-config.test.ts</automated>
  </verify>
  <done>
    - writeOpenclawConfig() creates valid openclaw.json with all sections
    - writeWorkspaceFiles() creates SOUL.md and IDENTITY.md with Murphy identity
    - sandbox-start.sh calls config writer before server start
    - All tests pass using temp directories
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire webhook handler to orchestrator and create integration test</name>
  <files>
    src/api/webhooks.ts,
    tests/lib/ai/agent-integration.test.ts
  </files>
  <action>
    1. Update `src/api/webhooks.ts`:
       - Add import: `import { chat } from '../lib/ai/orchestrator.js';`
       - Inside the existing `setImmediate()` block, after the current `console.log` line, add orchestrator invocation for call events:
         ```
         if (eventType === 'call.initiated') {
           const from = event?.data?.payload?.from ?? 'unknown';
           console.log(`[webhooks] Processing call.initiated from ${from}`);
           chat(
             [{ role: 'user', content: `Incoming call from ${from}. Greet the caller.` }],
             'greeting'
           ).then((response) => {
             console.log(`[webhooks] Orchestrator response: ${response}`);
           }).catch((err) => {
             console.error('[webhooks] Orchestrator error:', err);
           });
         }
         ```
       - This wires the webhook async handler to the orchestrator so that call.initiated events actually trigger agent processing, not just logging.
       - Keep the existing `console.log` for all event types (logging is still useful).
       - Do NOT change the HTTP response behavior (still returns 200 immediately).

    2. Create `tests/lib/ai/agent-integration.test.ts`:
       - Import app from src/server.ts, request from supertest
       - Mock the orchestrator module: `vi.mock('../../../src/lib/ai/orchestrator.js', () => ({ chat: vi.fn().mockResolvedValue('Hello! I am Murphy, an AI assistant.') }))`
       - Mock the webhook verifier to pass through in tests (follow pattern from existing tests/api/webhooks.test.ts)

       - Test: "POST /webhooks/telnyx with call.initiated event returns 200 and invokes orchestrator"
         - Send a mock Telnyx event with data.event_type = 'call.initiated', payload with call_control_id, from: '+15550001234', to
         - Assert response status 200 and body { received: true }
         - Wait briefly for setImmediate to fire (use `await new Promise(r => setTimeout(r, 50))` or `vi.useFakeTimers` + `vi.runAllTimersAsync`)
         - Assert `chat` was called with arguments matching: messages array containing 'Incoming call from +15550001234', task type 'greeting'
         - This proves the webhook-to-orchestrator pipeline is wired, not just HTTP ACK

       - Test: "POST /webhooks/telnyx with call.answered event returns 200 without invoking orchestrator"
         - Same pattern with event_type = 'call.answered'
         - Assert response status 200
         - Assert `chat` was NOT called (only call.initiated triggers orchestrator in this phase)

       - Test: "POST /webhooks/telnyx with unknown event type returns 200" (handler always ACKs)

       Note: The existing webhookRouter uses telnyxWebhookVerifier middleware. For integration tests, either:
       (a) Mock the middleware via vi.mock to pass through, OR
       (b) Set appropriate test headers that the verifier accepts in test mode
       Check how the existing tests/api/webhooks.test.ts handles this and follow the same pattern.

       Reference: @tests/api/webhooks.test.ts for the established testing pattern.
  </action>
  <verify>
    <automated>npx vitest run tests/lib/ai/agent-integration.test.ts</automated>
  </verify>
  <done>
    - src/api/webhooks.ts imports chat from orchestrator and calls it on call.initiated events inside setImmediate
    - Simulated call.initiated webhook returns 200 AND orchestrator chat() is invoked with greeting task
    - Simulated call.answered webhook returns 200 without triggering orchestrator
    - Unknown event type still returns 200 (ACK pattern)
    - Tests mock orchestrator and verify invocation, not just HTTP status
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete Phase 1.1 agent setup</name>
  <files>N/A</files>
  <action>
    Human verifies the complete OpenClaw agent setup by running the full test suite and inspecting generated config files.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>Human confirms all tests pass, config generates correctly, Murphy persona is complete, and gateway text response works</done>
  <what-built>
    Complete OpenClaw agent setup: Murphy persona (SOUL.md, IDENTITY.md), dual LLM routing (OpenRouter + Anthropic), tool registry with 4 stubs, config generator, webhook-to-orchestrator wiring, and integration tests.
  </what-built>
  <how-to-verify>
    1. Run the full test suite: `npx vitest run`
       - Expected: All tests pass (existing Phase 1 tests + new Phase 1.1 tests)
       - Specifically confirm agent-integration.test.ts passes with orchestrator invocation assertions

    2. Verify config generation works:
       ```bash
       npx tsx -e "import { writeOpenclawConfig, writeWorkspaceFiles } from './src/startup/openclaw-config.js'; writeOpenclawConfig(); writeWorkspaceFiles();"
       cat ~/.openclaw/openclaw.json
       cat ~/.openclaw/workspace/SOUL.md
       ```
       - Expected: Valid JSON config with gateway, agents, auth, plugins sections
       - Expected: SOUL.md contains Murphy's persona with AI disclosure

    3. Verify gateway text response (requires OpenClaw installed and API keys configured):
       ```bash
       openclaw agent --message "Hi Murphy, what can you help me with?" --agent murphy
       ```
       - Expected: Murphy responds with a greeting that includes AI disclosure
       - This verifies the full text path: config -> gateway -> LLM -> persona response

    4. Verify .env.example has OPENROUTER_API_KEY line
  </how-to-verify>
  <resume-signal>Type "approved" with confirmation that (a) all tests pass, (b) config generates correctly, and (c) gateway text response includes AI disclosure greeting. If gateway is unavailable, explain why and confirm the other two paths. Describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
Full test suite including new Phase 1.1 tests:
```bash
npx vitest run
```

Config generation sanity check:
```bash
npx tsx -e "import { writeOpenclawConfig, writeWorkspaceFiles } from './src/startup/openclaw-config.js'; writeOpenclawConfig(); writeWorkspaceFiles();" && cat ~/.openclaw/openclaw.json | head -20
```
</verification>

<success_criteria>
- openclaw.json is generated with correct structure (gateway, agents, auth, plugins)
- SOUL.md and IDENTITY.md written with Murphy persona
- sandbox-start.sh writes config before gateway start
- webhooks.ts setImmediate block calls orchestrator chat() on call.initiated events
- Integration test verifies orchestrator is invoked (not just HTTP 200)
- Full test suite passes
- Human confirms config output, test results, and gateway text response
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-openclaw-agent-setup/01.1-03-SUMMARY.md`
</output>
