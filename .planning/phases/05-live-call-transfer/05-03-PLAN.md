---
phase: 05-live-call-transfer
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/voice/call-state.ts
  - frontend/src/lib/voice/outbound-caller.ts
  - frontend/src/lib/tools/handlers/dispatch.ts
  - frontend/src/api/webhooks.ts
autonomous: true
gap_closure: true
requirements: [XFER-01, XFER-02, XFER-03, XFER-04]

must_haves:
  truths:
    - "tsc --noEmit completes with zero errors across the full project"
    - "frontend/src/ mirror files are byte-identical to their root src/ counterparts"
  artifacts:
    - path: "frontend/src/lib/voice/call-state.ts"
      provides: "CallState with 'transferred' stage and pendingBridge field"
      contains: "'transferred'"
    - path: "frontend/src/lib/voice/outbound-caller.ts"
      provides: "TRANSFER_BRIEF, bridgeToUser, transferred guard, normal_clearing cascade"
      exports: ["TRANSFER_BRIEF", "bridgeToUser"]
    - path: "frontend/src/lib/tools/handlers/dispatch.ts"
      provides: "Real transferCall with bridgeToUser import"
      contains: "bridgeToUser"
    - path: "frontend/src/api/webhooks.ts"
      provides: "Phase 5 webhook wiring: pendingBridge, call.bridged, transferred cleanup"
      contains: "call.bridged"
  key_links:
    - from: "frontend/src/lib/voice/call-state.ts"
      to: "src/lib/voice/call-state.ts"
      via: "exact content copy"
      pattern: "transferred"
    - from: "frontend/src/api/webhooks.ts"
      to: "src/api/webhooks.ts"
      via: "exact content copy"
      pattern: "bridgeToUser"
---

<objective>
Sync 4 stale frontend mirror files with their canonical root src/ counterparts to fix tsc --noEmit compilation failure (19 errors).

Purpose: Root tsconfig.json glob **/*.ts includes frontend/src/. Phase 5 updated only root src/ files, leaving frontend copies at Phase 4 state. This breaks the CI typecheck gate.

Output: All 4 frontend mirror files updated, tsc --noEmit passes clean.
</objective>

<execution_context>
@/Users/pmc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pmc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-live-call-transfer/05-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync call-state.ts, outbound-caller.ts, and dispatch.ts from root to frontend</name>
  <files>frontend/src/lib/voice/call-state.ts, frontend/src/lib/voice/outbound-caller.ts, frontend/src/lib/tools/handlers/dispatch.ts</files>
  <read_first>
    src/lib/voice/call-state.ts
    src/lib/voice/outbound-caller.ts
    src/lib/tools/handlers/dispatch.ts
    frontend/src/lib/voice/call-state.ts
    frontend/src/lib/voice/outbound-caller.ts
    frontend/src/lib/tools/handlers/dispatch.ts
  </read_first>
  <action>
Copy the EXACT content of each root src/ file to its frontend/src/ counterpart. These are mirror copies — the content must be byte-identical.

1. **frontend/src/lib/voice/call-state.ts** — Copy from src/lib/voice/call-state.ts. The key changes vs current frontend version:
   - Line 19 stage union: add `'transferred'` between `'calling'` and `'complete'` — becomes `'greeting' | 'name_capture' | 'intake' | 'consent' | 'searching' | 'calling' | 'transferred' | 'complete'`
   - Add field `pendingBridge: boolean;` after the stage field (line 20 in root)
   - Add `pendingBridge: false,` to initCall() defaults (line 59 in root)

2. **frontend/src/lib/voice/outbound-caller.ts** — Copy from src/lib/voice/outbound-caller.ts. The key changes vs current frontend version:
   - Add TRANSFER_BRIEF exported const function at lines 35-45 (before AI_INTRO)
   - Add bridgeToUser exported async function at lines 52-62 (after TRANSFER_BRIEF)
   - In handleProviderHangup: add transferred-stage guard at lines 378-385 (`if (state?.stage === 'transferred') { ... return; }`)
   - In handleProviderHangup: change cascadeCauses array from `['timeout', 'no_answer', 'user_busy']` to `['timeout', 'no_answer', 'user_busy', 'normal_clearing']`

3. **frontend/src/lib/tools/handlers/dispatch.ts** — Copy from src/lib/tools/handlers/dispatch.ts. The key changes vs current frontend version:
   - Import line 1: change `import { startOutboundCascade }` to `import { startOutboundCascade, bridgeToUser }`
   - TransferCallParams interface: add `call_control_id?: string` field
   - transferCall function: replace entire stub body with real implementation that gets CallState, validates providerCallControlId, and calls bridgeToUser()
  </action>
  <verify>
    <automated>cd /Users/pmc/Documents/DEV/OpenClaw-Vercel && diff src/lib/voice/call-state.ts frontend/src/lib/voice/call-state.ts && diff src/lib/voice/outbound-caller.ts frontend/src/lib/voice/outbound-caller.ts && diff src/lib/tools/handlers/dispatch.ts frontend/src/lib/tools/handlers/dispatch.ts && echo "ALL FILES MATCH"</automated>
  </verify>
  <acceptance_criteria>
    - diff src/lib/voice/call-state.ts frontend/src/lib/voice/call-state.ts produces no output (files identical)
    - diff src/lib/voice/outbound-caller.ts frontend/src/lib/voice/outbound-caller.ts produces no output (files identical)
    - diff src/lib/tools/handlers/dispatch.ts frontend/src/lib/tools/handlers/dispatch.ts produces no output (files identical)
    - grep "'transferred'" frontend/src/lib/voice/call-state.ts returns a match
    - grep "pendingBridge" frontend/src/lib/voice/call-state.ts returns a match
    - grep "TRANSFER_BRIEF" frontend/src/lib/voice/outbound-caller.ts returns a match
    - grep "bridgeToUser" frontend/src/lib/voice/outbound-caller.ts returns a match
    - grep "normal_clearing" frontend/src/lib/voice/outbound-caller.ts returns a match
    - grep "bridgeToUser" frontend/src/lib/tools/handlers/dispatch.ts returns a match
    - grep -v "STUB" frontend/src/lib/tools/handlers/dispatch.ts (no STUB text in file)
  </acceptance_criteria>
  <done>All three frontend mirror files are byte-identical to their root src/ counterparts. TRANSFER_BRIEF, bridgeToUser, pendingBridge, transferred stage, and normal_clearing are all present.</done>
</task>

<task type="auto">
  <name>Task 2: Sync webhooks.ts from root to frontend and verify tsc passes</name>
  <files>frontend/src/api/webhooks.ts</files>
  <read_first>
    src/api/webhooks.ts
    frontend/src/api/webhooks.ts
  </read_first>
  <action>
Copy the EXACT content of src/api/webhooks.ts to frontend/src/api/webhooks.ts. The key changes vs current frontend version:

1. **Import block** (lines 29-40): Add `bridgeToUser` and `TRANSFER_BRIEF` to the outbound-caller import — becomes:
   ```
   import {
     startOutboundCascade,
     tryNextProvider,
     handleProviderAnswer,
     handleAmdResult,
     handleProviderHangup,
     decodeClientState,
     parseAvailability,
     stopNarrationTimer,
     bridgeToUser,
     TRANSFER_BRIEF,
   } from '../lib/voice/outbound-caller.js';
   ```

2. **case 'call.speak.ended'** (lines 175-210): Replace the simple greeting-only handler with the new version that:
   - First checks if this is a provider-leg speak.ended via decodeClientState
   - If stage === 'provider-dial' and bridgeState?.pendingBridge is true, initiates bridge via bridgeToUser(callControlId, userCcid)
   - Has bridge failure catch block that cascades to tryNextProvider
   - Falls through to inbound user leg greeting logic only if not a provider-dial event

3. **Availability 'available' branch** (inside call.transcription provider-dial, lines 229-255): Replace `updateCall(userCcid, { stage: 'complete', currentProviderIndex: providerIndex })` with:
   - Build TRANSFER_BRIEF text from state
   - Set pendingBridge=true BEFORE speaking brief: `updateCall(userCcid, { pendingBridge: true, currentProviderIndex: providerIndex })`
   - Speak brief on provider leg (callControlId, not userCcid)

4. **Stage gate** (line 277): Change from `['searching', 'calling', 'complete']` to `['searching', 'calling', 'transferred', 'complete']`

5. **Add case 'call.bridged'** (lines 452-471): New event handler that:
   - Decodes client_state, filters to provider-dial stage only
   - Sets stage to 'transferred' via updateCall
   - Speaks goodbye on user leg: "Alright, you're connected! I'll leave you two to it — good luck!"

6. **call.hangup wasDialing** (line 507): Change from `['calling', 'complete']` to `['calling', 'transferred', 'complete']`

7. **call.hangup callStatus** (line 516): Change from `state.stage === 'complete'` to `state.stage === 'complete' || state.stage === 'transferred'`

8. **call.hangup connected_provider** (line 533): Change from `state.stage === 'complete'` to `(state.stage === 'complete' || state.stage === 'transferred')`

9. **Add user-hangup-during-transfer block** (lines 547-561): After call history persistence, check `(state.stage === 'calling' || state.pendingBridge) && state.providerCallControlId` — speak apology to provider and hang up after 3s delay

10. **Cleanup condition** (line 563): Change from `state.stage !== 'complete'` to `state.stage !== 'complete' && state.stage !== 'transferred'`

After copying the file, run `npx tsc --noEmit` from the project root to verify zero compilation errors.
  </action>
  <verify>
    <automated>cd /Users/pmc/Documents/DEV/OpenClaw-Vercel && diff src/api/webhooks.ts frontend/src/api/webhooks.ts && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - diff src/api/webhooks.ts frontend/src/api/webhooks.ts produces no output (files identical)
    - grep "bridgeToUser" frontend/src/api/webhooks.ts returns a match
    - grep "TRANSFER_BRIEF" frontend/src/api/webhooks.ts returns a match
    - grep "call.bridged" frontend/src/api/webhooks.ts returns a match
    - grep "pendingBridge" frontend/src/api/webhooks.ts returns a match
    - grep "'transferred'" frontend/src/api/webhooks.ts returns a match
    - npx tsc --noEmit exits with code 0 (zero errors)
  </acceptance_criteria>
  <done>frontend/src/api/webhooks.ts is byte-identical to root src/api/webhooks.ts. tsc --noEmit passes with zero errors across the full project.</done>
</task>

</tasks>

<verification>
1. All 4 frontend mirror files are byte-identical to root src/ counterparts: `diff src/lib/voice/call-state.ts frontend/src/lib/voice/call-state.ts` (and same for the other 3 files) produces empty output
2. `npx tsc --noEmit` exits with code 0
3. No STUB text in frontend/src/lib/tools/handlers/dispatch.ts
4. The 19 TypeScript errors from VERIFICATION.md are resolved
</verification>

<success_criteria>
- tsc --noEmit passes clean (zero errors) across the full project
- All 4 frontend/src/ mirror files match their root src/ counterparts exactly
- Phase 5 verification truth #10 ("TypeScript compiles cleanly across the full project") is now satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/05-live-call-transfer/05-03-SUMMARY.md`
</output>
